# MTP (Multi-Token Prediction) — 計画

QAT ウェイト対応・Vision 対応に続く 3 つ目の大改修。
`google/gemma-4-26B-A4B-it-qat-…-assistant` ドラフターを使った
propose-and-verify (投機デコード) を本ランタイムに入れる。

**現在地: M7 (32 スロット = 16GB Mac の運用点) 完了 ([27-M7-RESULTS.md](27-M7-RESULTS.md))。**
**受入結果は [RESULTS_MTP.md](../../RESULTS_MTP.md) (リポジトリ直下)。**

**M8 は提案 ([28-M8-PROPOSAL.md](28-M8-PROPOSAL.md)) と、その果実 B の前提検定
([29-M8-B-PROBE.md](29-M8-B-PROBE.md)) まで。**32 スロットの壁 (ブロック 82 ms の
うち 33 ms が expert 読み出しの露出) に対し、**層 L の hidden に層 L+1 の router を
先行適用すると、層 L+1 が読むことになるエキスパートの 66% に先に名前が付く**
(実測)。全部先読みするとバイトが 1.72 倍で自滅するが、予測重みの上から 1 本に
絞れば的中 70% / バイト 1.07 倍で、**導出でブロック 82 → 約 66 ms (約 30 t/s)**。
**計器までが入っていて、先読みを fetch に渡す実装はこれから** (設計は 29 §6)。

M7 は運用点を **80 → 32 スロット**に移した (`Scripts/demo/serve.py` の設定、16GB 機向け)。
ここでは M6 までの結論がそのまま成り立たない — **32 スロットのブロックは
GPU ではなく SSD で律速している** (ブロック 1 回が 469 MB 読む)。
verify の中で効いたのは 3 つ: 層の expert union を**タイルごとでなく 1 プラン**で置く、
**常駐エキスパートのタイルを先頭に切って読み出しの陰で走らせる**、
**router の k 行を 1 dispatch に畳む**。**32 スロットで 20.8 → 23.6 t/s**、
**48 スロットで 24.7 → 28.9 t/s**、出力は 3 つとも 1 バイトも変わらない。
25-M5.6 §5 が残した `moe` の 11 ms は**重み帯域の不足ではなかった** (時間はエキスパート数でなく
(token, expert) ペア数に比例する) ので、床の立て方ごと 27 §6 で置き換えた。
**目標だった「32 スロットで 30 t/s」には届いていない** (23.6)。30 t/s の運用点は
**48 スロット** (peak 6.83 GB、16GB 機に収まる)。

M6 まで ([26-M6-RESULTS.md](26-M6-RESULTS.md)):
サーバーにも `--draft-block-size` が入り、pi 形のコーディング要求 (tools 宣言 +
streaming + プロンプトキャッシュ) で **1.386 倍**、出力は 2,213 文字が 1 バイト差なし。
日本語の散文は受理長 1.058 で **1.0 倍** — **受理長はタスクで 2 倍動く** (26 §2)。

M5.6 まで ([25-M5.6-RESULTS.md](25-M5.6-RESULTS.md)):
**採点基準 v1 は 1.136 → 1.336 倍** (bs=4)。**22 §5 が凍結した目標 1.33 に届いた。**
速度比は 1.177 → **1.419**、受理長は 1.885 で不変
(ドラフター側は 1 行も触っていない — 動いたのは費用側だけ)。
24-M5.5 が残した 4 項目の 1 番目 — ブロックの attention が KV を k 回読むこと — を
**k 行を 1 回の split-KV で通すカーネル** (1 threadgroup = (q head, chunk)、
1 simdgroup = 1 行、barrier 無し) に畳んだ。`attn.swa` は **約 10 → 約 1.5 ms**、
層前半は 24 → **15 ms**、ブロックの GPU busy は 59 → **50 ms**、
層あたりの dispatch は **2k → 2 本**。ただし `--verify-block` は 11/1 → **9 PASS / 3 FAIL** で、
増えた 2 本は top-1 マージン 1.0e-3 の競り合った 1 位置の argmax 反転である (25 §4)。

M5.5 まで ([24-M5.5-RESULTS.md](24-M5.5-RESULTS.md)):
**採点基準 v1 は 1.050 → 1.136 倍**。22 §5 の中止ライン 1.10 の上に戻った。

23-M5 §7 が挙げた次の 1 段 (`wait.front` = ブロックの 76%) は**否定された**。
GPU の占有率を測る計器 (`GPUQueueOccupancy`) を足すと、温まったブロックは
**GPU busy 70 ms / wall 86 ms = 81%** で、その待ちの大半は前層 MoE と自層 front の
本物の GPU 仕事である。比べるべき相手は decode 側の占有率 (**busy 25.7 ms / wall 38 ms**)
で、**ブロックは decode の 2.7 倍の GPU 仕事**をして 2.9 トークンを出していた。
差は attention に集中していて、原因は**プロンプト用の query-block カーネルを
k≤8 に通していたこと** (生きている simdgroup が 16 個 = 512 スレッド、split-KV なし)。
ブロックの attention を decode の split-KV カーネルに 1 行ずつ渡すと、
温まったブロックは **93 → 72 ms**、GPU busy **70 → 59 ms**。
副産物として、15-M4 §2 が測った「チャンク経路対 decode 経路」の分岐が
`bench/m.json` では**消えた** (共通接頭辞 87 文字 → 215/215 全一致)。

M5 (ループ統合) まで ([23-M5-RESULTS.md](23-M5-RESULTS.md)):
22-GOAL-RESET §7 の指示どおり M4.x の最適化を止めてループを先に作り、
**採点基準 v1 を初めて実行した** (1.05 倍)。ループ実測はマイクロベンチと 2 点で
食い違い、`verify(4)` は 1.78 → **2.63** decode ステップ、受理長は a(4) 1.60 → **1.90**。
感度表の入力を **F=1.33 / M=0.31 / draft=0.079** に差し替えると予測が実測を再現する
(`bench/mtp_budget.py`)。**この F/M は M5.5 の改修前の値である** (未再測定)。

M4.8 まで:
19-M4.7 §7 の「次の 1 段」だけをやった: k 行カーネルは**重みを 1 回しか読んでいなかったが、
活性化は k 回読んでいた**。affine バイアス項の活性化和 (重み行に依存しないのに lm head の
262144 行が全部計算し直していた)、活性化のロードと変換、そして実行時値のままだった
ブロック幅 — この 3 つを外すと、head と Q/K/V 射影は **k=4 が k=1 と同じ時間**になる
(461 MB を 3.41 ms = 135 GB/s、帯域床)。routed MoE には別口で「常に 8 行ぶんに展開」が
あり、これも k で固定した。ブロックの GPU スパンは k=4 で **68 → 52 ms**、
費用は **2.02 → 1.78** decode ステップ (同一セッションの A/B) で、
**`verify(4)` が 16-M4.5 §6 のゲート 1.9 を初めて下回った** (ただし M5 のループ実測は
2.63 で、この水準はマイクロベンチのものだった — 23-M5 §4)。

M4.7 まで:
expert cache のスロットを 48 → **80** に広げると、18-M4.6 が「隠す」対象にしていた
読み出しが**消える**: k=4 の `fetch` は 27 → 9 ms、ブロックは壁時計 76.5 ms に対し
GPU スパン 72.5 ms で**ほぼ GPU 律速**になった。費用は k=4 で **2.38 → 2.16**、
k=8 で 4.25 → 3.57 decode ステップ。同時に 2 つの前提が崩れた: (a) ブロックが expert
和集合を畳む利益は**産出トークンで割ると 12%** しかない (18-M4.6 §2 は検証トークンで
割っていた)、(b) 費用の k に対する伸びが M0 のモデルより急なので、
**`bs=4` 固定 (14-M3.5 §5) は最良ではない** — ただし M4.8 で費用の伸びが緩み、
m は bs=4 が上に戻った (20-M4.8 §5)。

M4.6 まで:
17-M4.5 §4 が「層ごとのホスト往復と dispatch 数」と読んだ残差は、ホスト側の
新しいプロファイルで割ると **expert の読み出し待ち**だった (k=4 で 1 ブロック 106 ms の
うち 40 ms、ミス 58 回 = 0.20 GB)。読むバイトは変えずに、層の全タイルの読み出しを
grouping の直後にまとめて発行し、**常駐タイルを先に encode して**その裏に隠した。
費用は k=1 で 1.60、k=4 で **2.74 → 2.34** decode ステップ。
14-M3.5 の受理長で引くと **math が初めて黒字 (0.97 → 1.12 倍)**。
**16-M4.5 §6 のゲート (1.15 / 1.9) にはまだ未達。**
M4.5 (17-M4.5) で入った k 行カーネル (routed MoE・射影・shared expert・ヘッド・router、
カーネル単位で decode と bit 一致) はそのまま効いている。

M4 では verify パス (`verifyBlock`) と KV 巻き戻し (`rewind`) が入り、出口条件 3 つが
成立した ([15-M4-RESULTS.md](15-M4-RESULTS.md)): 全位置の argmax が 1 トークンずつの
`produce` と **256/256 一致**、巻き戻し後の続きが「巻き戻さなかった世界」と
**16/16 一致**、わざと壊した巻き戻し 3 通りは全部 FAIL する。数値は bit 一致ではないが、
**その差は verify が作っていない** — 投機要素ゼロの k=1 ブロックが同じ値を出すので、
正体はチャンク経路とスカラー decode 経路の既存差である (15-M4 §2)。この差は M4.5 でも
残っている (17-M4.5 §5) — 消すには層の前半も decode に寄せる必要がある。

M4 が測った「費用が想定の 3.8 倍」の正体は 16-M4.5 が言うとおり
**借りたカーネルの性能**だった。verify は prefill チャンク経路を借りており、
そこは 1 スレッドグループが 64 行の出力タイルを持つ。4 行のために 64 行ぶん計算するので
**演算強度が稜線を 5 倍超え、帯域律速だった decode が演算律速に変わる**。
M4.5 でこれを k 行カーネルに直した結果、**k 行に増やした演算は実際にタダだった**
(17-M4.5 §4 の GPU 実測)。残ったのはホスト側の往復と dispatch 数である。

受理率側は参照実装と並んでいる (14-M3.5)。残っているのは**ターゲット側の実行効率**
1 つで、M4.5 はその置き場所の訂正だった — 半分は直り、半分 (層の前半) が残っている。
decode 経路 (`produce`) は今も無変更で、`verifyBlock` を呼ばない実行のコストはゼロ。

## ゴール (2026-08-17 に確定)

QAT ウェイト・Vision・MTP の 3 改修が揃った状態として、**Server** で
**単発の Vision タスクを Reasoning ON で** `pp ≥ 200 t/s` / `tg ≥ 30 t/s` で回す。
GUI は対象外。**主眼は wall time で、t/s は目安**。
受理率はタスク依存なので、**cpp / haiku / math / story / vis の 5 本**で測り、
先読み長 k のトレードオフを併せて記録する。
**採点基準は [22-GOAL-RESET.md](22-GOAL-RESET.md) に凍結した (v1)。**
`pp 200` は tower 固定費のため構造的に未達 (M0 §0-5)。`tg 30` は単位を固定して

| | v1 |
| --- | ---: |
| **目標** | ゴールタスク 8 枚 (`sample_imgs/`、Reasoning ON、temp 0、80 スロット、生成長の上限なし) の **end-to-end 壁時計比の中央値 1.33 倍** |
| **中止** | **1.10 倍**を切る → 報告して停止 |

と読む。比は必ず**同一セッションの MTP on/off 交互 A/B** で取り、絶対 t/s は載せない。

**採点は M5 のループ実測だけで行う。**M4.x に点数は付けない — 15-M4 から 21 までの
速度比はすべて `verify(k) × a` の**導出**で、別条件で測った 2 つの proxy の積を
成績表に載せたために 6 本が互いを差し替え続けた (22 §1)。**倒れたのは水準だけで、
同一セッション A/B で取った差分は 1 件も倒れていない** (22 §2)。

改修候補は着手前に**感度表**に通す (`./bench/mtp_budget.py`、22 §3): 現状
**F (ブロック 1 回の固定費) が唯一の大きなレバー** (半減で e2e 1.29、床付近で 1.58)、
**受理長 a はもう効かない** (vis を math 並みにしても 1.08)、
**タスク依存は二次** (4 タスクの幅 0.16〜0.20 に対しレバーは 0.6)。
現在地は **e2e 1.00 前後** (21 §6 の導出。M5 未実装のため実測はまだ無い)。

表記は PLAN 系と同じ: **実測** / **導出** / **未確認**。

## 読む順

| # | 文書 | 役割 |
| --- | --- | --- |
| 1 | [01-CHECKPOINT.md](01-CHECKPOINT.md) | ドラフター側の事実 (重み・config・アルゴリズム)、ターゲットとの整合、未解決の仕様質問 |
| 2 | [02-RUNTIME-FIT.md](02-RUNTIME-FIT.md) | 性能のベースライン実測値と、既存ランタイムとの対応。そのまま使える資産 / 新規に書くもの 4 点 |
| 3 | [03-DESIGN.md](03-DESIGN.md) | 設計判断 D1〜D8。フォーマット・verify API・KV 巻き戻し・受理規則・ループ配置・CLI/Server |
| 4 | [04-PHASES.md](04-PHASES.md) | M0〜M6 の分解、出口条件、受入 10 ゲート、中止条件 |
| 5 | [05-RISKS.md](05-RISKS.md) | リスクと未確認事項の一覧 |
| 6 | [10-M0-RESULTS.md](10-M0-RESULTS.md) | **M0 の実測。**速度上限の式、expert 和集合、タスク別の受理上限、ゴール条件のベースライン |
| 7 | [11-M1-RESULTS.md](11-M1-RESULTS.md) | **M1 の実測。**フォーマット拡張と repacker、ピンの再確認、`--add-draft` の実行、旧ランタイム拒否 |
| 8 | [12-M2-RESULTS.md](12-M2-RESULTS.md) | **M2 の実測。**ドラフター forward 単体、参照実装確定 (transformers 5.10.4)、FP16 誤差床、検出力 3 件 |
| 9 | [13-M3-RESULTS.md](13-M3-RESULTS.md) | **M3 の実測。**実ドラフターの受理率 (bs=2/3/4、temp 0/1.0)、参照実装との差、費用側の天井、group サイズ 32/64 の衝突、**go と bs=3 の決定**。受理率まわりの数字と §6 の規約比較は 14-M3.5 が差し替えた |
| 10 | [14-M3.5-RESULTS.md](14-M3.5-RESULTS.md) | **M3.5 の実測。**`attnOut` のバッファオーバーラン、参照実装の直接実測 (`--spec-draft-n-max 1`)、実入力リプレイ、入力ごとのアブレーション、**受理率の回復と bs=4 の決定**。§5 の速度比は 15-M4 §4 が無効化した |
| 11 | [15-M4-RESULTS.md](15-M4-RESULTS.md) | **M4 の実測。**`verifyBlock` と `rewind`、全位置 logits の突合 (argmax 256/256、数値差の正体)、巻き戻しの検出力 3/3、**verify の費用の初測定**。§4 の実測値は有効だが、**§4-1 の当てはめと §5 の含意は 16-M4.5 が差し替えた** |
| 12 | [16-M4.5-PLAN.md](16-M4.5-PLAN.md) | **M4.5 の計画。**15-M4 の再解釈 (roofline で 4 点全部を説明)、M0 費用モデルの復権、k 行カーネルの予測、限界費用の訂正、作業順 a〜e、測り方とゲートの書き換え |
| 13 | [17-M4.5-RESULTS.md](17-M4.5-RESULTS.md) | **M4.5 の実測。**k 行カーネル 4 種 (routed MoE / 射影 / shared expert / ヘッド) と decode router、層あたりの往復削り。**decode との bit 一致検査**、段ごとの A/B、GPU と壁時計の内訳、ゲート未達の内訳と次の 1 段。**§4 の残差の読み (往復と dispatch) は 18-M4.6 §2 が差し替えた** |
| 14 | [18-M4.6-RESULTS.md](18-M4.6-RESULTS.md) | **M4.6 の実測。**ホスト側プロファイル (`TF_PREFILL_HOST_PROFILE`)、**残差の正体は expert I/O** という訂正、外した案 (和集合の一括読み)、読み出しの先行発行と常駐タイル優先の encode 順、**math の黒字化**と残り 4 項目 |
| 15 | [19-M4.7-RESULTS.md](19-M4.7-RESULTS.md) | **M4.7 の実測。**スロット 48/64/80 のスイープ、ブロック要求列の机上シミュレータ (`bench/mtp_expert_sim.py`)、**ボトルネックが expert I/O から GPU へ移ったこと**、GPU 時間が k に比例している内訳、**bs=4 固定の再検討**。**§2 は 18-M4.6 §2 の「和集合を畳む利益」の割り算を差し替えた** |
| 16 | [20-M4.8-RESULTS.md](20-M4.8-RESULTS.md) | **M4.8 の実測。**k 行カーネルが活性化側に払っていた 3 つの k 倍 (affine 和・ロードと変換・実行時のブロック幅)、カーネル単体のベンチ (`--rows-bench`) と t=5 の崖、融合 greedy ヘッドと routed MoE への同じ適用、**`verify(4)` のゲート達成**と速度比の引き直し。**§0 の「ゲート達成」は 22 §2 が採点から取り下げた — 生きているのは同一セッション A/B の差分のほうである** |
| 17 | [21-GOAL-CONDITION-RESULTS.md](21-GOAL-CONDITION-RESULTS.md) | **ゴール条件の実測。**probe に画像 / Reasoning / 冷えたキャッシュを入れた (`--verify-image` / `--verify-thinking` / `--verify-cold`)、**幅ごとに歩く位置を揃える測り方の訂正**、text warm 対 ゴール条件の費用、**「ブロックが SSD ミスを畳む」利益の実測 (冷えたキャッシュではゼロ)**、k=1 が運用点ではないことの整理、速度比の引き直し。**§6 の速度比は導出であって採点ではない — 22 §1 が位置づけを直した** |
| 18 | [22-GOAL-RESET.md](22-GOAL-RESET.md) | **目標設定のやり直し。**6 本が互いを差し替え続けた原因の特定 (導出を成績表に載せた / 中止条件を採点に昇格させた)、**倒れたのは水準だけで差分は倒れていない**という切り分け、**感度表** (`bench/mtp_budget.py`) — 着手前に「完全勝利したらスコアが何倍か」を出す、採点基準 v1 (ゴールタスク 8 枚の生成長と decode 比率の実測)、目標値と運用ルール |
| 19 | [23-M5-RESULTS.md](23-M5-RESULTS.md) | **M5 の実測。**ループ統合 (`--draft-block-size`)、**採点基準 v1 の初実行 (1.05)**、ゲート 1 の分岐は投機ではなく経路差、マイクロベンチとループ実測の食い違いと感度表の入力の差し替え。**§7 の「次の 1 段は `wait.front`」は 24-M5.5 §2 が否定した** |
| 20 | [24-M5.5-RESULTS.md](24-M5.5-RESULTS.md) | **M5.5 の実測。**GPU 占有率の計器 (`GPUQueueOccupancy`、ブロックと decode の両方)、**`wait.front` = ホスト往復という 23 §7 の読みの否定**、1 行ブロック 対 decode の段別突き合わせ、**ブロックの attention が decode のカーネルの 9 倍**という特定、decode の split-KV カーネルへの行ごとの付け替え、**スコア 1.050 → 1.136**、ゲート 1 の分岐が消えたこと、残っている床との差 |
| 21 | [25-M5.6-RESULTS.md](25-M5.6-RESULTS.md) | **M5.6 の実測。**k 行を 1 回の split-KV で通す attention カーネル (1 simdgroup = 1 行、barrier 無し、combine は decode のものを流用)、段別の A/B と dispatch 数、**スコア 1.136 → 1.336 (目標到達)**、ゲート 1 は全一致のまま `--verify-block` が 2 本増えて落ちたこと、24-M5.5 §2 の帯域床の訂正 (KV head は 8)、残り (`moe` とホスト往復) |
| 22 | [26-M6-RESULTS.md](26-M6-RESULTS.md) | **M6 の実測。**サーバーの `--draft-block-size` (起動時に確保する `SpeculativeScratch`、ドラフター無しは起動失敗、repetition penalty 付き要求の退避、完了ログの `accept=`)、pi 形の要求での A/B (code 1.386 / 2 ターン対話 1.51・1.47 / 日本語散文 1.007)、**ゲート 9 の合格** (`cached_tokens` が on/off で一致)、**ゲート 1 はサーバーでも分岐すること**と `TF_MTP_DRAFTS=0` による切り分け、**受理長のタスク依存が感度表の外側にある**という訂正 |
| 23 | [27-M7-RESULTS.md](27-M7-RESULTS.md) | **M7 の実測。**32 スロット (16GB 機の demo 設定) の verify — 層 union の 1 プラン化・常駐タイル先頭切り・router の k 行畳み込みで **32 スロット 20.8 → 23.6 t/s / 48 スロット 24.7 → 28.9 t/s** (出力 1 バイト不変)、`moe` の床 (25-M5.6 §5) の否定 (時間はペア数比例)、**32 スロットの壁は expert I/O** (469 MB/ブロック、履歴からの先読みは当たらない)、採点基準 v1 は 32 スロットで 1.394 |
| 24 | [28-M8-PROPOSAL.md](28-M8-PROPOSAL.md) | **M8 の提案 (未着手、実測なし)。**「物理限界か」の資源別判定表、`moe` 28 ms の再読 (行 1 本の限界費用 ≈ エキスパート全読み — 帯域でなく発行/占有率律速の疑い)、**隠れ状態ベースの投機的エキスパート先読み** (27 §7 の「予測できない」は履歴ベースの話で、router の先行適用は未試行)、llama.cpp から取るもの (simdgroup_matrix MoE / `p_min` 適応ドラフト / mmap+bytesNoCopy 常駐 / `llama-bench` での外部校正)、却下案と理由の表、**着手前に潰す計測 4 本** (counters 半日 / 的中率 1 日 / bs スイープ半日 / 校正半日) |
| 25 | [29-M8-B-PROBE.md](29-M8-B-PROBE.md) | **M8 果実 B の前提検定 (実測)。**層 L の router 入力に層 L+d の router 重みを当てる計器 (`RouterPreviewProbe`、`TF_MTP_ROUTER_PREVIEW`、出力不変)、**層 L+1 の実ミスの 66% に先に名前が付く**こと、**エキスパート番号の層間相関はランダムと同じ (5.9% 対 6.2%)** という代案の否定、予測を全部読むと 1.72 倍のバイトで自滅すること、**予測重みの上から N 本に絞る積み上げ** (N=1 で的中 70% / バイト 1.07 倍)、導出したブロック 82 → 63〜66 ms、**先読みを fetch に渡す実装の設計と必要な新 API 2 つ** (投機用プラン / 飛行中ハンドルの待ち位置) |
| 26 | [30-M8-B-PREFETCH.md](30-M8-B-PREFETCH.md) | **M8 果実 B の実装 (実測)。**29 §6 の引き継ぎをそのまま実装 — 投機用プラン (`planSpeculativeExperts`: 使用回数を上げない / 直近のプランを追い出さない)、`.route` 段の頭でのハンドル待ち、`TF_MTP_EXPERT_PREFETCH`。**予測は設計どおり当たる** (N=1 で 70%、`declined=0`、`wait.prefetch`≈0、出力 md5 不変) のに **32 スロット −3.5% / 48 スロット −1.5% と遅くなる**こと、その内訳 (予測そのものの費用 −1.0〜1.2% と、増えたバイトの費用)、**29 §5 の導出が外した点 = 32 スロットは すでに I/O 帯域が床**で前倒ししてもバイトが減らないこと、48 スロットでは fetch が 42.6 → 40.2 ms と縮むが `wait.front` +3.7 ms に食われること、N/d スイープ (増やすほど単調に悪い) |
| 27 | [31-M8-A-ROWS-BENCH.md](31-M8-A-ROWS-BENCH.md) | **M8 果実 A の計器 (実測、実装は未着手)。**routed MoE の rows カーネル 2 本を verify ブロックの時間軸から外して測る `--moe-rows-bench` (合成重み・モデル無し・I/O 無し、`gate/up` と `down` を別々に、再現性 1% 未満)、**27 §6 の 3 つの「不変」は測り方の限界だった可能性**、**`down` が `gate/up` の 2 倍高くバイトは半分** (F=704 のスカラー末尾が疑わしい)、**`cap` ミスマッチの崖** (cap=2 と 3 の間、1 行のブロックが 3 行の仲間のせいで +70%)、`encodeStreamedRows` がタイルの最大行数で PSO を選ぶため production が毎回払っていること、**`down` を崖で 2 分割して `moe` −17% の導出**と実装の引き継ぎ、固定値テスト 11 件の追随 (フルスイート全通過) |
| 28 | [32-M8-A-ROWS-SPLIT.md](32-M8-A-ROWS-SPLIT.md) | **M8 果実 A の実装 (実測)。**31 §7 の引き継ぎをそのまま実装 — `encodeStreamedRows` の `down` を行数 2 で 2 ディスパッチに分割 (`TF_MTP_ROWS_DOWN_SPLIT`)。**出力 md5 不変 (30 プロセス) / `moe` 30.11 → 27.99 ms (−7.0%) / GPU 全体 −4.1%** で分布が重ならないこと、**それでも t/s は −0.2% (ノイズ)**。31 §6 の −17% が外れた理由が 3 つとも**タイルの実形状** (production は 16 でなく **5.68 エキスパート/タイル**、**タイルの 43% は最大 2 行で分割が発火しない**、追加ディスパッチが **+2.3%**) であること、積み直すと計器の予言 −2.07 ms と実測 −2.12 ms が 3% で合うこと、**完全バケット化は 2 分割より遅い** (31 §6 の導出は符号が逆)、**48 スロットでも fetch が call の 58% で I/O が床**という 30 §5 の再確認、分割/非分割のビット一致テスト |
| 29 | [33-M8-IO-FLOOR.md](33-M8-IO-FLOOR.md) | **32 §7 の 2 本目 (I/O を床から外す) の検定 (実測、本番コード無改変)。**新しい pread プローブ `bench/io_depth_probe.c` (同時エキスパート数 × 分割数、`F_NOCACHE` 切り替え) で、**読みの分割はどの深さでも効かない**こと、**27 §7 の「8 本 = 8.90 GB/s」がページキャッシュ込み**で冷たい device の天井は **6.0〜6.5 GB/s** であることを固定。本番の発行深さ = 層のミス数 (`concurrentPerform(iterations: plan.misses.count)`) なので**キャッシュを良くするほど帯域が落ちる** (32/48/64 スロットで 5.91/4.90/3.36 GB/s)。冷たい ABBA で **48 → 64 スロットは io −17% / t/s −3% (動かない) / メモリ +1.8 GB**、32 → 48 は io −32% / t/s +14% で**膝は 48 と 64 の間**。出ない理由は **GPU 床** (`gpuq busy` が 1 ms も動かず `fetch` の減分が `wait.front` に移る。48 スロットでも中央のブロックは busy/call 70〜86%)。**結論として 32 §7 の順序は逆で、果実 A (`down` の F=704 末尾) が先** |

事実の出所は 2 系統ある。**リモート (HF) 側**はドラフター重みの調査 (2026-08-17 取得。
テンソル全数・SHA-256 一致・config・参照実装ソースは `scratch/mtp-ref/` に取得済み)、
**ローカル側**は commit `156667c` のソースと `RESULTS_VISION.md` / `RESULTS_QAT.md` の実測。
リモート側は上流で変わりうるので、M1 の取得時に再確認する。

## この計画の運用ルール

`PLAN_VISION.md` が 1,728 行になり、途中から誰も通読できなくなった反省で分けている。

- **1 文書 = 1 論点。**目安 200 行以内。超えたら分ける。
- **進捗は文書を増やして書く。**既存文書の先頭に `0-A` `0-B` … と積んでいく形は取らない。
  M0 の結果は `10-M0-RESULTS.md`、M1 は `11-M1-RESULTS.md` というように
  **段ごとに新しいファイル**を作り、この索引に 1 行足す。
- **設計文書 (01〜05) は書き換える。**結果が出て前提が変わったら追記ではなく本文を直す。
  「読むべき最新の設計」が常に 1 か所にあるようにする。訂正の履歴は git log が持つ。
- 受入結果は最後に `RESULTS_MTP.md` (リポジトリ直下、PLAN §6 準拠) にまとめる。
  `RESULTS_QAT.md` / `RESULTS_VISION.md` と同じ場所・同じ体裁にする。
  → **書いた ([RESULTS_MTP.md](../../RESULTS_MTP.md))。この計画はここで閉じる。**
