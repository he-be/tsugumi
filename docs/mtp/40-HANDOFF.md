# 40. 引き継ぎ — MTP スレッドの再開入口

最終更新: 2026-08-20。ブランチ `macos15-support`、最新は 48 (D の P-1〜P-4 実測)。
**ツリーはクリーン。**
**47 の D プローブは 48 で実行済み** — P-1 PASS / P-2 PASS / P-3 FAIL / **P-4 はゲートの上**。
**次の 1 手は「D を実装するか保留にするか」のユーザー判断待ちである** (48 §8)。
(a) `moe` バケットの 3 分割は引き続き**保留**。
44/45/46 (W1/W2/W3) は**別の枝**で、次の 1 手を消費していない — 43 が読んだ帯域床を
バイト側から検定し (44)、その結論を製品に入れ (45)、残ったコード側のバイトを
測って閉じた (46) だけで、`moe` バケットの 3 分割はそのまま残っている。

**45 で推論経路が変わった。**M9 を通じて `Sources/TurboFieldfare/` は無改変だったが、
45 が `sym` スキームを入れた。**出力はバイト一致なので過去の測定値は生きるが、
速度の数字は 45 以降 8.8% 速い側で取られる。**

**この文書は MTP スレッドを再開するための唯一の入口である。**
リポジトリ直下の `TODO.md` は**別スレッド (サーバー再実装) が使っている**ので混同しないこと。
索引は [README.md](README.md)、運用ルールもそこにある。

## 1. 再開のしかた

```
@docs/mtp/40-HANDOFF.md @docs/mtp/README.md
MTP の続きを進めて。
```

読む順は **40 (本書) → 43 → 42 → 41 → 39 → 38 → README の索引**。
**36 は通読しないこと** (3,321 行。冒頭の「読み方」だけ読む)。

## 2. 今どこにいるか

M9 は 34 が P1〜P7 を置き、35 (外部校正) → 36 (台帳) → 37 (並べ直し) →
**38 (register の検定) → 39 (48 スロット台帳) → 41 (N2、decode の分解) →
42 (N3、採点基準 v2) → 43 (P2、`moe` の帯域判定)** と進んだ。
枝として **44 / 45 / 46 (W1〜W3、重みの形式)** がある — 34 の P リストには無い。
**推論経路 (`Sources/TurboFieldfare/`) は M9 を通じて 1 行も変えていなかった
(45 で変わった)。**
**製品に入った変更は 45 の `sym` の 1 つだけ**で、あとは「どの前提が嘘だったか」の
地図である。W の枝は 46 でコード側も閉じたので、**残っているのは 44 §7 が開けた
「`head` と dense 射影の `sym9`」1 枚**である (46 §6)。

### 確定していること (**実測**、反復あり)

| | 中身 | 出典 |
| --- | --- | --- |
| 台帳の形 | `step(k) = base + Σc_i + d·k` は turbo でも成立。**内挿専用** (bs=6 以上で 10〜15% 破れる) | 36 §3b/§4d |
| `d` | **0.064 base/本**。画像 8 枚で ±3%、温度・thinking・スロット数で動かない | 36 §3b |
| `c(slots)` | 16/24/32/48 = **0.633 / 0.563 / 0.440 / 0.287**。**画像に依らない** (別画像で再現) | 39 §1 |
| 最適 bs | **2〜3。48 スロットでも 2 のまま伸びない** | 39 §2 |
| 既定 bs=4 | 動かす根拠なし。採点条件で差 +0.05% (95% 区間 ±2%) | 36 §17 |
| L1 の破れ | 壁時計 ≠ Σmax(io, busy)。正しくは **Σbusy + α(bs)·Σio** | 36 §14/§16 |
| α(bs) | bs=2 で 0.840 → bs=8 で 0.331。register にもスロット数にも依らない | 36 §16 |
| **`2^H` 軸** | `hit% = 108.67 − 0.6553·2^H` が **R² = 0.993**。`register` はこの連続量の代理ラベル | 38 §3 |
| 果実 A | **閉じない。**48 スロット MTP off の decode は 40.98 ms/tok で、**expert I/O は 21%** | 39 §4 |
| スロットの効き | 16→48 で I/O −38% でも壁時計は **−5.2%** (変換率 43%) | 39 §5 |
| **残り 25 ms の正体** | **GPU busy。**残差 24.8〜28.3 対 busy 25.4〜26.4 ms/tok。ホスト往復ではない | 41 §2 |
| GPU busy | **register でもスロット数でも動かない** (25.4〜26.4、`attn` は 11.04〜11.13)。動くのは `idle` だけ | 41 §1 |
| `c` の中身 | **89〜94% が routed MoE。**`attn`/`head`/`tail` は bs に依らない (1 位置 6.4〜6.9 GPU ms) | 41 §4 |
| `F` の中身 | GPU 15.3〜18.2 ms/ブロック。最大項 `attn` 11.0 の内訳は **qkv 38% / oproj 19% / post 19%、attention 本体は 9.6%** | 41 §4/§5 |
| 投機の効き | 産出トークン 1 本の GPU busy が **25.4 → 18.1 ms (−29%)** (bs=0 → bs=2) | 41 §3 |
| **register は入力ではない** | 同じプロンプトで seed だけ変えると io/tok が **23% 動く**。カテゴリのセルは均質でない | 41 §1a |
| **運用点は 32 スロット** | `Scripts/demo/serve.py:62`。48 は `c(slots)` のトレンドのために振った点で、**採点条件ではない** | 42 §1 |
| **採点基準 v2** | 16 枚 / 32 スロット / bs=4 / temp 0。**スコア 1.415** (目標 1.33 を満たす)。t/s 比 1.539 | 42 §3 |
| **e2e 比を動かすのは長さ** | 生成長比 対 壁時計比 = **−0.910**。`io` は +0.282、`hit%` は −0.364 しかない | 42 §4 |
| 温度 0 でも分岐する | off と on で文字まで一致するのは **18 本中 4 本**。25 §4 の argmax 反転 | 42 §3 |
| **`moe` は帯域床ではない** | production の `down` は **89.2 GB/s (床 135 の 66%)**。末尾の無い形状だけが 125〜131 | 43 §2 |
| `gate/up` は床 | 全形状で **133〜137 GB/s**。末尾があっても割らない。**触る余地なし** | 43 §2 |
| **外れは `down` の 6 群の末尾 1 か所** | F=768 は F=704 より **25.6% 速い** のに**バイトは 9% 多い** | 43 §2 |
| **末尾は行あたりの費用 = `c`** | 2 本目の行が 末尾なし **+1.0〜9.1 µs** に対し F=704 は **+52.5 µs** (限界費用の **84%**) | 43 §3 |
| 繕いの見積もり | **production のタイル幅と行数混合の上で** `down` **−44%**、rows 2 本で **−23%** | 43 §4a |
| **QAT の `bias` は情報ゼロ** | `bias == -8*scale` が bf16 のビットとして **7 億 8817 万群すべて**で成立。対照のベースラインは 34〜38% 外れる | 44 §1 |
| **床はバイトで買える** | `bias` を落とすと `gate/up` は **−9.7%** (バイト比 −10.0%) で **GB/s は動かない** (135.3 → 134.8)。出力は half 1 ulp まで同一 | 44 §4 |
| `scale` は詰められない | 8 bit 符号は出力 RMS 誤差 **0.70%**、9 bit 符号は可逆でも `down` **+15.5%** (末尾に償却先が無い)。**運用のタイルでは両方 `sym` 単独に負ける** | 44 §5 |
| **`sym` は採用済み** | 出力は temp 0 / 256 tok × 3 本で**バイト一致**、ルーティングも同一。運用点で **t/s +8.8% / peak −0.40 GB / io −11.7%**、インストール 14.71 → 13.28 GiB | 45 §3/§4 |
| ハーネスの穴 | カーネル検定 69/69 が通ったまま実モデルが壊れうる。`checkRoutedMoE` は **decode の gate/up を通らない** | 45 §3a |
| **コードは 3.45 bit** | INT4 コードの order-0 エントロピーは **3.4509 bit/weight** (252 億コードの全数)。**28 §5 の「縮まない見込み」は外れ** — 理想 rANS で **3.951 bpw / −12.2%**。表は役割ごとに 1 つで足り、隣接相関はゼロ | 46 §2 |
| **エントロピー符号化は閉じる** | 取り分 io 1.73 ms/tok に対し、展開器に **10.35 GB/s の出力**が要る。**読み出しのほうが展開より速い。**却下理由が「縮まない」から「展開が追いつかない」に差し替わる | 46 §4 |
| 適応ビット幅も閉じる | 32 コードの群が 3bit の固定窓に収まるのは **0.00%** (min オフセット付きでも 0.02%)、2bit は 0.00% | 46 §3 |
| **実効帯域はページキャッシュの関数** | ミス数を固定 (temp 0 で決定論) して生成長だけ変えると **3.87 → 6.18 → 9.09 GB/s** (24/96/256 tok)。**生成の頭ほど遅い。**「2 歩目だけ特別」ではない (1 歩目 64 ミス 対 以降 6〜65) | 46 §5a |
| **プレッシャーの出どころ** | run 中は空きが **0.06 GiB に張り付き**、ファイルバックが **+3.4 GiB**。プロセスの peak は 4.49 GB で動かない。decode の pagein は 0.66 GB/s で、読むバイト 2.49 GB/s の **4 分の 1** | 46 §5b |
| スロットは私有の二重持ち | 30 層 × 32 スロット × 3.36 MB = **3.22 GB**。同じバイトが OS のページキャッシュにも載る。決着は `F_NOCACHE` の 1 手 (**未着手**) | 46 §5b/§6 |
| **residency の粒度はバッファ** | 冷たい状態から 8 エキスパートを名指すコマンドバッファ 1 本が常駐にするのは **1,640 / 26,240 ページ = 26.9 MB ちょうど**。層単位の 1 バッファなら **429.9 MB**。3 様式 × 5 反復 × 2 run | 48 §1 |
| **`MAP_PRIVATE` は CoW を割る** | `PROT_READ` でも GPU が読んだページが**全部 anonymous + dirty** になる。**`MAP_SHARED` + `PROT_READ` は 0 / 0** ⇒ D は `MAP_SHARED` | 48 §2 |
| **暖かい側のコピーは 0 になる** | `mmap` はカーネルがマッピングをその場で読むので転送段が無い。対照の暖かい `pread` は 8 エキスパートで 0.94〜1.21 ms | 48 §3 |
| **冷たい側は先読み無しで 2.5 倍負ける** | 13.1 対 5.1 ms (8 エキスパート)。`F_RDADVISE` を足すと **6.22 対 5.36 ms = 1.16 倍** (paired 1.204、ゲートは 1.20) | 48 §4/§5 |
| **`msync(MS_INVALIDATE)` は sudo 無しで効く** | ページキャッシュが落ちる (張り直して 0/26,240 常駐、冷たい直列タッチ 0.73 GB/s)。**`madvise` では落ちない** | 48 §6 |
| `full-sha256` は 12 GB 読む | 既定の `--verification` は初回の層オープンで全レイヤをハッシュする。**TTFT +5.4s、ページキャッシュを一度洗う。**`trusted-install` で外れる | 46 §5c |

### 次の 1 手 — **D を実装するか保留にするか (ユーザー判断待ち)**

**プローブは終わっている。**設計は 47、実測と判定は
[48](48-D-P1-P4-MMAP-RESIDENCY.md)、判断の材料は 48 §8、
実装する場合の作りは 48 §9 にある。**本節は要約である。**

| | 結果 | 47 §7 の閾値 |
| --- | --- | --- |
| **P-1** 粒度 | **PASS (決定的)**。8 エキスパート = 1,640 ページ = 26.9 MB ちょうど | 約 27 MB |
| **P-2** 暖かい費用 | **PASS (決定的)**。転送段が消えて **0** | `pread` の 1/3 以下 |
| **P-3** 冷たい費用 (先読み無し) | **FAIL**。13.1 対 5.1 ms = **2.5 倍** | 1.2 倍以内 |
| **P-4** `F_RDADVISE` 併用 | **ゲートの上**。pooled **1.161** / paired **1.204** / カーネル差し引き **1.115** | 1.2 倍以内 |

**47 §7 の判定表はこの行を「実装する (先読みを併せて)」に置いているが、
P-4 の余白は 0.04 で測定の再現幅と同じ桁である** (round 単位の比は 1.06〜1.28)。
**⇒ 進むか保留かはユーザーの決定** (47 §7 の最終行がそう書いている)。

**進めるなら実装は `PreadExpertStreamer` の差し替えに閉じる** (47 §10 の着手点、
48 §9 の作り: `MAP_SHARED`+`PROT_READ` / 層ごとに 1 マップ / エキスパート単位に
128 バッファ / `F_RDADVISE` は必須)。**形式変更は不要**、受け入れは 45 と同じ
バイト一致ゲート。

**保留にするなら**、47 §11 の「ロードマップが書き換わる」は発火せず、
次は下の (a) か 41 §5 の dense 射影に戻る。

#### 保留にした (a) — `moe` バケットを 3 分割する計器

**理由**: (a) の判定条件は「rows 2 本が `moe` の 7 割以上か」で、これを
**I/O 律速の条件下で取ると数字の意味が変わる**。D が当たれば同じ計器が
そのまま使える (対象が I/O から GPU に移るだけ) ので、**順序を逆にしない**。
以下は保留時点の記述をそのまま残したものである。

**P2 は 43 で終わり、34 §2b-1 の二択は「`down` の末尾繕い」に決まった。**
繕いの見積もりは **production のタイル幅と行数混合の上で `down` −44% /
rows カーネル 2 本で −23%** (43 §4a)。

**しかし実装の前に 1 つ潰す。**2026-08-20 のやり取りでユーザーが (a) を選んだ:

> **rows カーネル 2 本が、ブロックの `moe` GPU 時間の何割なのか。**
> 41 §4 は `moe` を 11.16 ms/ブロック (bs=2) と測ったが、**その中を
> rows / router / gather に割っていない。割り算の分子が無い。**

**これが 32 の再演を止める関門である。**32 は分子を知らないまま実装して
`moe` −7.0% / GPU −4.1% を得て **t/s −0.2% (ノイズ)** で終わった。
43 §4a はタイル形状の交絡を潰したが、この分子は潰していない。

#### 分かっている着手点 (調査済み、コードは 1 行も書いていない)

- **計器は `Sources/TurboFieldfare/Runtime/Prefill/PrefillGPUProfile.swift` (107 行)。**
  `Stage` (attn/shared/moe/tail/head) と、`TF_PREFILL_GPU_PROFILE=2` でだけ
  解決される `Detail` (attn を 9 分割) を持つ。41 §5 が使ったのは後者である。
- **`.moe` はタイル 1 個のコマンドバッファ単位で計時されている** —
  `RealForwardRunner.swift:1450` / `:2198` / `:2216` の
  `try waitProfiled(.moe, tile.commandBuffer)`。**1 本の中に gate/up と down の
  両方が入っている**ので、そのままでは割れない。
- タイルの encode は
  `Sources/TurboFieldfare/Kernels/Prefill/MoE/PrefillGroupedRoutedMoE.swift:709`
  の `encodeStreamedRows`。

#### 方式 — **41 §5 の前例に倣う**

`Detail` に `moe.gateup` / `moe.down` / `moe.route` / `moe.reduce` を足し、
**detail モードのときだけ**タイルの encode を段ごとのコマンドバッファに切る。
**前例どおり壁時計は膨らむので比率だけを読む** (`PrefillGPUProfile` の
doc comment がそう書いている)。本番経路は detail モードでのみ分岐する。
(代案は `MTLCounterSampleBuffer` で dispatch 境界にタイムスタンプを置く手。
膨らまないが 34 §6 が「恒久化するなら」と書いた側で、実装は重い。)

#### 判定条件を先に置く

- **rows 2 本が `moe` の 7 割以上**なら、43 §4a の −23% はブロック GPU busy の
  **−6% 以上**になる。**繕いを実装する。**
- **5 割未満**なら、−23% は **−4% 以下**で **32 が t/s を動かせなかった水準と同じ**。
  **その場合は繕いを実装せず、41 §5 が開けた `F` の dense 射影の札
  (qkv 38% / oproj 19% / post 19%) に移る。**
- 41 §4 の内訳では `moe` はブロック GPU busy の **37.5%** (11.16 / 29.76、bs=2)。

#### その後 (繕いを実装する場合)

- 的は `prefill.metal:1184-1198` の末尾ループ (32 レーンのうち 16 しか生きておらず、
  活性化のスカラー half 2 本を**行ごと**に払う)。**限界費用の 84% がここ** (43 §3)。
- **これは `F` ではなく `c` を削る** ので、**実装後は採点条件で bs スイープを
  取り直す** (41 §6 / 42 §2d が保留した宿題)。
- **総和順序が動くのでビット一致が壊れる**
  (`PrefillRoutedMoERowsVsDecodeTests`、31 §3)。**本数は見積もっていない。**
- **本番で測り直すまで t/s を主張しない** (32 スロットは SSD 律速。27 §7)。

**残っている札 1 枚 (未着手)**: **`F` の dense 射影**。`attn` 11.0 ms/ブロックの
**76% が qkv/oproj/post の dense 行列積**である (41 §5)。**見積もり未了。**
43 と同じ手 (形状を振って帯域床と突き合わせる) が使えるはずで、計器は `--rows-bench`。

**N4/N5/N6 は 37 §3 のまま。**

### P6 の残り (42 で片付いた分と、残っている分)

**片付いた**: md5 台帳の版管理 (`bench/mtp_goal_v2_images.tsv`。ドライバが起動時に
突き合わせて落ちる)、冷温プロトコルの script 化 (暖機 / クールダウン / 他プロセス検査 /
`iogpu.wired_limit_mb` の記録)、36 §9g の 3 規則と 39 §3 の 4 つ目 (中央値・平均・分布を
並べる / 反復を目盛りに使う / ドリフト検定 5%) を**採点ドライバに実装**。

**残っている**: 同じ 3 規則を `bench/mtp_cd_sweep.py` 側にも入れること
(あちらは head/tail base を**表示するだけ**で、5% 超の run を捨てていない)。

## 3. 触ってはいけないもの / 決まっていないもの

- **本プロジェクトはこの MBP (M3 Pro 18 GiB) 専用である** (ユーザー確定 2026-08-20)。
  **他機への配布予定は無い。**したがって README が名乗る「8 GB 機で動く」
  ("even the 8 GB ones"、"validated target is an 8 GB M2 MacBook Air") は
  **設計上の制約ではない**。**運用点は暖かい regime** (ページキャッシュが
  12 GiB のエキスパート集合の大半を持てる状態) であり、46 §5a の
  「冷たい側 3.87 GB/s」は運用点ではない。
  **⇒ 46 §4 の「エントロピー符号化は閉じる」は条件付けを要しない。**
- **目標 1.33 / 中止 1.10 (22 §5) は凍結。**書き換えない。
  **v1 の 8 枚は失われた**ので v1 の絶対値表は再現できない (復元は試みない。
  ユーザー確定 2026-08-20)。**代わりに 42 が採点基準 v2 を切った** —
  16 枚 / 32 スロット / bs=4 / temp 0 で **スコア 1.415**。
  **v1 の 1.336 や 27 §2c の 1.394 と絶対値で並べてはいけない。**
- **採点画像は `bench/mtp_goal_v2_images.tsv` が凍結している。**変えるなら v3 を切る。
  画像の実体はリポジトリ外 (`~/Pictures/sample_imgs`、写真 46 枚 + ロゴ類 4 枚。
  `TF_SAMPLE_IMGS` で差し替え、`.gitignore:53-54` で追跡外)。
  **10〜20KB のロゴ類 4 枚はキャプション課題の代表にならない**ので除外してある。
- **運用点は 32 スロット** (`Scripts/demo/serve.py:62`、peak 4.97GB)。
  **48 スロットには `sudo sysctl iogpu.wired_limit_mb=14336` が要る** (再起動で 8192 に戻る)
  が、**48 は運用点ではない** — `c(slots)` のトレンドを見るために振った点である。
- **llama.cpp は再実行しない** (35 の校正値を既定値として読むだけ)。

## 4. 落とし穴 (実際に踏んだもの)

1. **n=1 のセルに解釈を書かない。**36 は 188 測定から 3,321 行を書き、
   看板の発見 (§11/§12) が n=1 だった。**反復 3 未満のセルは表に数字を置くだけにする。**
   38/39 はこの規則で書いて **115 / 116 行**に収まっている。
2. **`mtp_cd_sweep.py` の head/tail base は外れ値検出器である。**
   `|head/tail| > 5%` の run は捨てる (39 §3。実際に 1 本捕まえた)。
   36 §9g の 3 規則 (隣接ペア / 反復 3 以上 / 中央値・平均・分布を並べる) に続く 4 つ目。
3. **48 スロットは切り替え直後にページイン費用を払う** (6.78GB 常駐)。
   スイープの前に暖機を 1 本入れる (39 §3)。
4. **温度の既定 (temp 1.0 / top-k 64 / top-p 0.95) がそのまま運用点である。**
   `--temperature 0` を明示すると運用点から外れる。36 §11 はこれで n=1 のまま
   運用点でない条件を測っていた。
5. **macOS の `/bin/bash` は 3.2 で連想配列が無い** (`declare -A` が落ちる)。
6. **`sleep` を Bash ツールのコマンドに直接書くとタスクごと落ちる。**
   待つならスクリプト内に入れてバックグラウンドで投げる。
7. **生ログは必ずファイルにリダイレクトし、`bench/mtpNN/` にコピーしてコミットする**
   (`bench/logs/` は gitignore。`*.log` も gitignore なので `git add -f` が要る)。
8. **`[prefill gpu]` と `[prefill host]` は `tail` と `head` を両方持つ (意味は別物)。**
   同じ辞書に入れると先に読んだほうが上書きされる (41 §7-8 で実際に踏んだ)。
9. **計器は無料ではない。**`TF_PREFILL_HOST_PROFILE=1` + `TF_PREFILL_GPU_PROFILE=1` は
   decode を **1.9〜2.3% 遅くする** (41 §7-1)。速度比を取る run には立てない。
10. **カテゴリ軸を裏口から戻さない。**38 §3 が `register` を `2^H` に置き換え、
    41 §1a が「その `2^H` すら入力ではない」を出した。それでも「主軸となる register を
    決める」という形で 1 度戻しかけた。**選べるのはプロンプトであって、
    出てくるトークン列ではない。**
11. **条件を跨いで数字を突き合わせない。**41 の絶対 t/s は
    thinking off / CLI / 計器 ON。ゴールの `tg ≥ 30` は Reasoning ON / Server。
    **並べた瞬間に嘘になる。**22 §1 が「導出を成績表に載せた」と呼んだ失敗の別の形である。
    **2026-08-20 に 2 度目を踏みかけた** — 39 §2 の「最適 bs=2」(48 スロット / temp 1.0 /
    192 tok) を採点条件 (32 スロット / temp 0 / 生成長無制限) に持ち込もうとした (42 §2d)。
12. **「運用点」を勝手に決めない。**運用は **32 スロット**である
    (`Scripts/demo/serve.py:62`)。39/41 が 48 で測ったのはトレンドのためなのに、
    40 の初版はそれを「運用点」と呼び替えていた。**要件は serve.py とユーザーが持つ。**
13. **反復は画像 (セル) を増やす代わりにならない。**temp 0 では同一画像の run は
    決定論的で、反復が測るのは装置の再現幅だけである。**中央値の n を増やすのは
    セルを増やすこと。**反復は目盛りとドリフト検定に使う (42 §2e)。
14. **e2e 壁時計比は生成長の分岐に開いている** (42 §4)。temp 0 でも on/off が
    文字まで一致するのは 18 本中 4 本で、**セル 1 個の比は相関 −0.910 で長さに従う。**
    **中央値は頑健だが、セル 1 個の比を本文で引いてはいけない。**
15. **プローブが自分で機械を汚す。**48 のゲートは run をまたいで比が
    1.16 → 1.31 と単調に悪化した。犯人は同じプロセスの前半で走る
    `MAP_PRIVATE` の腕で、1 trial ごとに 430 MB を anonymous にして
    圧縮と swap を溜めていた。**判定に使う数字は、汚す腕と同じプロセスで取らない**
    (`--mmap-probe-gate-only`)。
16. **`madvise(MADV_DONTNEED)` はページキャッシュを落とさない。**落ちるのは
    このタスクの PTE だけで、張り直すと全ページが戻る。落とすのは
    **`msync(..., MS_INVALIDATE)`**。ただし**同じファイルに他のマッピングが
    開いていると 0 を返して落とさない**ので、測る側を張る前に行い、
    `mincore` で落ちたことを確認して retry する (48 §6)。
17. **カーネルの計器は production の群サイズで走らせる。**`--group-size` の既定は
    **64** で、production は **32** である (`ArchConfig.gemma4_26B_A4B`)。
    43 の 1 回目は 64 で走り、末尾の群数が半分になって別の話を測っていた。

## 5. 揃っていない実測どうしの食い違い (**未確認**、揃えるために数字を動かしていない)

1. **計器と本番の `c` が 2.2〜2.7 倍離れている** (costProbe 0.343 対 本番 0.128〜0.155)。
   thinking を外しても消えなかった。**N5 の主題。** (36 §13-2)
2. **32 スロットの投機比 (A 1.355 / B 1.424) が 48 の比 (1.311 / 1.376) より
   両画像とも高い**のに、`c` は 48 のほうが低い。32 側が n=1 で確定していない。 (39 §6-2)
3. **同じ計算の壁時計比が夜をまたぐと 8.6% 動く。**産出トークン・受理・hit は
   ビット単位で再現するのに採点量だけが動く。 (36 §9c(b))
4. **`code` bs=6 の α (0.509) が `ja_prose` bs=6 (0.399) より高い。**
   bs 以外に効く軸が残っている。 (36 §16 末尾)
5. **参照実装との比較は片側が未定義。**llama.cpp の 26.9 ms/tok は
   `llama-bench` の合成列で register が定義されていない。 (39 §4)
6. **48 スロットが 32 スロットより遅く出た** (ja_prose、42.26 対 41.00 ms/tok)。
   ただし 32 を後に回しており、ドリフト (+0.85%) と交絡している。**ABBA が要る。**
   39 §6-3 の符号の不一致と同じ場所である。 (41 §7-2)
7. **`busy` が register でわずかに動く** (+0.5〜0.9 ms/tok)。動くのは `shared` と
   `moe` だけで `attn`/`head` は動かない。並行 pread との競合か、タイル数か。 (41 §7-3)

## 6. 計器と一次資料の在処

| | |
| --- | --- |
| スイープ | `bench/mtp_cd_sweep.py` (温度・クールダウン 20s の既定を持つ) |
| 採点条件 A/B | → 下の「採点 v2」。暖機 / クールダウン / ドリフト検定 / 共変量を持つ |
| 費用曲線 | `.build/release/TurboFieldfareKernelCheck --verify-cost-only` |
| ルーティング | `TurboFieldfareCLI --dump-expert-trace` + `bench/mtp36/an3.py` (`2^H`) |
| ブロック台帳 | `TF_PREFILL_HOST_PROFILE=1` |
| GPU の段別 | `TF_PREFILL_GPU_PROFILE=1` (`=2` で層前半を 9 分割、**帰属専用**) |
| **採点 v2** | `bench/mtp_goal_ab.py` (引数なしで 18 ペア / 約 40 分)。台帳 `bench/mtp_goal_v2_images.tsv` |
| **rows カーネル** | `--moe-rows-bench` (production 形状)、**`--moe-rows-shape-sweep`** (還元次元を振る。`--group-size 32 --moe-rows-r <1..8>`) |
| **mmap residency** | **`--mmap-residency-probe`** (`--mmap-probe-trials` / `--mmap-probe-repeats` / **`--mmap-probe-gate-only`** / `--mmap-probe-model`)。P-1〜P-4 を 1 本で回す |
| **重みの形式** | **`--bpw-probe`** (形状を固定して affine / sym / sym8 / sym9 を振る。`--group-size 32`)。`bench/check_bias_identity.py <model.gturbo>` が格子側を検定する |
| **スキームの入れ替え** | `TF_FORCE_AFFINE_SCHEME=sym\|affine` (**計器であって設定ではない**)。データを固定して導出だけ差し替える。45 §3a の切り分けはこれで付いた |
| 集計 | `bench/mtp38_analyze.py` (register)、`bench/mtp39_analyze.py` (48 スロット)、`bench/mtp40{,b,c}_analyze.py` (N2)、`bench/mtp42_analyze.py` (N3) |
| N2 のドライバ | `bench/mtp40_n2{,b,c,d}_driver.sh` (bs=0 / bs=2,3 / 細分 / bs=4) |
| 一次資料 | `bench/mtp36/` (M9-A)、`bench/mtp38/` (register 検定)、`bench/mtp39/` (N1)、`bench/mtp40{,b,c,d}/` (N2)、`bench/mtp42/` (N3)、`bench/mtp43/` (P2)、`bench/mtp44/` (W1)、`bench/mtp45/` (W2)、**`bench/mtp48/` (D のプローブ)** |
