# 40. 引き継ぎ — MTP スレッドの再開入口

最終更新: 2026-08-20。ブランチ `macos15-support`、最新は **52** (D の prefill の負けを潰した)。

**この文書は MTP スレッドを再開するための唯一の入口である。**
ただし**索引から導けるものは持たない** — 各文書の要約は [README.md](README.md)、
測定の結論と次の 1 手は各文書自身にある。**ここにあるのは 4 つだけ**:
決まっていること (§3)、踏んだ落とし穴 (§4)、揃っていない食い違い (§5)、計器の在処 (§6)。
**§3〜§5 は他文書から番号で引かれている** (`40 §4-11` など) ので、
**項番号を変えない。足すのは末尾**。

リポジトリ直下の `TODO.md` は**別スレッド (サーバー再実装) が使っている**ので混同しないこと。

## 1. 再開のしかた

```
@docs/mtp/40-HANDOFF.md @docs/mtp/README.md
MTP の続きを進めて。
```

読む順は **40 (本書) → README の索引 → 索引が指す文書だけ**。
**36 は通読しないこと** (3,321 行。冒頭の「読み方」だけ読む)。

## 2. 今どこにいるか

| | |
| --- | --- |
| 最新 | **52** (**51 の ttft の負けは消えた**。prompt prefill は両腕とも `F_RDADVISE` を出しておらず、pread だけが 7.58 並列で深度を代替していた。advise を足すと運用点で **tok/s ×1.331 / ttft ×0.77 / peak −3.20 GB** と 3 軸とも勝つ) |
| 製品に入った変更 | **45 の `sym`**、**51 の D + 52 の advise (2026-08-20 に既定 on。§2a)**。**出力はバイト一致なので過去の測定値は生きるが、速度の数字は 45 以降 8.8% 速い側で、D の既定化以降はさらに速い側で取られる** |
| 開いている W の枝 | 44 §7 の「`head` と dense 射影の `sym9`」1 枚 (46 §6) |
| 開いている GPU の枝 | 41 §5 の dense 射影 (`attn` 11.0 ms/ブロックの 76% が qkv/oproj/post)。**見積もり未了** |
| N4/N5/N6 | 37 §3 のまま |

### 2a. **D は既定になった** (2026-08-20、ユーザー判断)

`TF_EXPERT_MMAP` / `TF_EXPERT_MMAP_ADVISE` は「計器」をやめ、**既定 on の設定**に
なった。CLI・サーバー・Mac アプリ・KernelCheck が同じ既定を共有する
(経路を決めるのは `Model.swift` の `useMmap:` 1 か所)。

| | |
| --- | --- |
| 既定 | **mmap + residency set**、`F_RDADVISE` は **mmap の腕のときだけ**出す (52 §5: pread には効かない) |
| 外し方 | **`TF_EXPERT_MMAP=0`** で 51 までの私有スロット + `pread` に丸ごと戻る。`TF_EXPERT_MMAP_ADVISE=1/0` は advise だけを腕と無関係に固定する (52 §5 のドライバがそう回す) |
| 目印 | CLI の footer の **`[expert mmap layers=30 …]`** の行、サーバーの ready 行の **`expert_io=mmap`**、デモの見出しの `expert mmap` のピル |
| テスト | `PreadExpertStreamer` を直に作るテストは **`useMmap: false` が既定**なので `pread` のまま (17 テスト green) |
| 確認した (**n=1 の疎通確認。スコアではない** — 40 §4-1) | CLI (17×23 の説明、96 tok、32 スロット、temp 0、連続 2 run): mmap `peak 1.29 GB / 24.88 tok/s`、`TF_EXPERT_MMAP=0` `4.90 GB / 16.79 tok/s`。表示された範囲の生成文は一致。**測定として読むのは 52 §4 の表であり、この 2 行ではない** |

**bench の腕は動かない** — `bench/mtp5*` は `TF_EXPERT_MMAP` を明示的に 0/1 で
渡す。**動いたのは advise の既定のほう**なので、advise 以前のログを再現する
ドライバには `--advise off` / `TF_EXPERT_MMAP_ADVISE=0` を書き足した
(`mtp51_mmap_ab.py` に `--advise auto|on|off`、`mtp52_slot_sweep.sh`、
`mtp52_single_arm.sh`)。**`--advise auto` は今の製品の形を測る。**

### 次の 1 手 — **既定にした。残っているのは既定の形での検証**

**52 で ttft の負けは消えた。**51 §5 が「帰属していない」と置いた prompt prefill の
×1.52 は、(a) ABBA の汚染 (約 0.14) と (b) **キュー深度**だった。prompt prefill は
**両腕とも `F_RDADVISE` を出しておらず** (advise の呼びは decode 経路の
`RealForwardRunner.swift:3127` だけ)、pread はミスを `concurrentPerform` で
**7.58 本同時**に投げることで深度を代替していた。mmap ではそれが
`requestResidency()` 1 本に潰れる。**明示的に先読みを頼むと消えた** (52 §3/§4)。

運用点 (math / 256 tok / 32 スロット / temp 0)、ブロック ABBA、定常 n=10、
**生成文は 4 腕ともバイト一致**:

| | prefill io | ttft | tok/s | peak |
| --- | ---: | ---: | ---: | ---: |
| pread (今の製品) | 0.876 s | 1.236 s | 26.03 | 4.50 GB |
| pread + advise | 0.901 | 1.253 | 25.63 | 4.50 |
| mmap (51) | 1.158 | 1.617 | 31.65 | **1.30** |
| **mmap + advise** | **0.537** | **0.960** | **34.13** | **1.30** |

**advise は `pread` には効かない** (×1.03、ノイズ内) ので取り分は D 固有である
(52 §5)。**揃えた上で `pread+adv` → `mmap+advise` は ×1.331 / ×0.77 / −3.20 GB。**

**51 §2 の +18.0% は控えめな数字だった** — ABBA の汚染と先読みの欠如を両方
含んでいる。**残っている 3 つ、優先順**:

1. **既定の形での検証** (§2a で既定にしたので、残りは確認である) —
   **`iogpu.wired_limit_mb` を既定 8192 に戻した run**、**サーバー経路
   (`Scripts/demo/serve.py`) の数字**、**`story` と MTP (bs=4) での advise**。
   いずれも**未実施**。**`--expert-cache-slots` の振り直しは済んだ**
   (52 §1: prefill はスロットに依存しない。要求 1963 / ヒット 0% が 16/32/64 で不動)。
   **既定が mmap になったので、48 スロットの `wired_limit` 前提も測り直す価値がある**
   (私有スロットが無いぶん peak は 1.3 GB で、スロット数はもう占有を動かさない)。
2. **decode も速くなった理由を分ける** (52 §6) — 19.56 → 27.41 GB/s。decode 経路は
   既に advise を出しているはずなので、**`shouldSkipRDAdvice` が落としている**か
   **タイミングが悪い**かのどちらか。`rdadvisePolicyMode` を振っていない。
   **これは D と独立に製品が取りこぼしている可能性がある。**
3. **51 §4a の内訳** — 「コピーが消えた」と「ページキャッシュが 3.2 GB 広くなった」を
   分けていない。**mmap の腕に 3.2 GB の anonymous バラストを持たせる腕**で決まる。
   52 §4 で prefill も暖かいと分かったので**この腕の重みは上がった**。

**`F_NOCACHE` の枝は開いたまま** — 短い生成で **tok/s +27.1%** (50 §2) だが
運用点では **−7.7%** で**符号を決める軸を特定していない** (50 §6)。
**D が入るならこの枝の意味も変わる** (D はページキャッシュを迂回しない)。

### 保留 (a) — `moe` バケットを 3 分割する計器

**P2 は 43 で終わり、二択は「`down` の末尾繕い」に決まった** (43 §4a: `down` −44% /
rows 2 本で −23%)。**実装の前に分子を 1 つ潰す** — rows 2 本がブロックの `moe` GPU
時間の何割か。41 §4 は `moe` を 11.16 ms/ブロック (bs=2、GPU busy の 37.5%) と
測ったが、その中を rows / router / gather に割っていない。**32 はこの分子を
知らないまま実装して t/s −0.2% (ノイズ) で終わっている。**

- **判定**: rows 2 本が **7 割以上**なら 43 の −23% はブロック GPU busy **−6% 以上**
  ⇒ 繕いを実装する。**5 割未満**なら −4% 以下 = 32 と同じ水準 ⇒ 繕わず 41 §5 の
  dense 射影に移る。
- **着手点**: 計器は `PrefillGPUProfile.swift` (`Detail` に `moe.gateup`/`moe.down`/
  `moe.route`/`moe.reduce` を足し、**detail モードのときだけ**タイルの encode を
  段ごとのコマンドバッファに切る。41 §5 の前例どおり**壁時計は膨らむので比率だけ読む**)。
  `.moe` の計時は `RealForwardRunner.swift:1450`/`:2198`/`:2216`、
  encode は `PrefillGroupedRoutedMoE.swift:709`。**コードは 1 行も書いていない。**
- **繕う場合の注意**: 的は `prefill.metal:1184-1198` の末尾ループ (限界費用の 84%、43 §3)。
  **`F` ではなく `c` を削る**ので**採点条件で bs スイープを取り直す** (41 §6 / 42 §2d)。
  **総和順序が動くのでビット一致が壊れる** (31 §3、本数は未見積もり)。
- **D が当たれば同じ計器がそのまま使える** (対象が I/O から GPU に移るだけ) ので、
  **順序を逆にしない。**

### P6 の残り

**残っているのは 1 つ** — 36 §9g の 3 規則 + 39 §3 の 4 つ目を
`bench/mtp_cd_sweep.py` 側にも入れること (採点ドライバには実装済み。
あちらは head/tail base を**表示するだけ**で 5% 超の run を捨てていない)。

## 3. 触ってはいけないもの / 決まっていないもの

- **本プロジェクトはこの MBP (M3 Pro 18 GiB) 専用である** (ユーザー確定 2026-08-20)。
  **他機への配布予定は無い。**したがって README が名乗る「8 GB 機で動く」
  ("even the 8 GB ones"、"validated target is an 8 GB M2 MacBook Air") は
  **設計上の制約ではない**。**運用点は暖かい regime** (ページキャッシュが
  12 GiB のエキスパート集合の大半を持てる状態) であり、46 §5a の
  「冷たい側 3.87 GB/s」は運用点ではない。
  **⇒ 46 §4 の「エントロピー符号化は閉じる」は条件付けを要しない。**
- **目標 1.33 / 中止 1.10 (22 §5) は凍結。**書き換えない。
  **v1 の 8 枚は失われた**ので v1 の絶対値表は再現できない (復元は試みない。
  ユーザー確定 2026-08-20)。**代わりに 42 が採点基準 v2 を切った** —
  16 枚 / 32 スロット / bs=4 / temp 0 で **スコア 1.415**。
  **v1 の 1.336 や 27 §2c の 1.394 と絶対値で並べてはいけない。**
- **採点画像は `bench/mtp_goal_v2_images.tsv` が凍結している。**変えるなら v3 を切る。
  画像の実体はリポジトリ外 (`~/Pictures/sample_imgs`、写真 46 枚 + ロゴ類 4 枚。
  `TF_SAMPLE_IMGS` で差し替え、`.gitignore:53-54` で追跡外)。
  **10〜20KB のロゴ類 4 枚はキャプション課題の代表にならない**ので除外してある。
- **運用点は 32 スロット** (`Scripts/demo/serve.py:62`、peak 4.97GB)。
  **48 スロットには `sudo sysctl iogpu.wired_limit_mb=14336` が要る** (再起動で 8192 に戻る)
  が、**48 は運用点ではない** — `c(slots)` のトレンドを見るために振った点である。
- **llama.cpp は再実行しない** (35 の校正値を既定値として読むだけ)。

## 4. 落とし穴 (実際に踏んだもの)

1. **n=1 のセルに解釈を書かない。**36 は 188 測定から 3,321 行を書き、
   看板の発見 (§11/§12) が n=1 だった。**反復 3 未満のセルは表に数字を置くだけにする。**
   38/39 はこの規則で書いて **115 / 116 行**に収まっている。
2. **`mtp_cd_sweep.py` の head/tail base は外れ値検出器である。**
   `|head/tail| > 5%` の run は捨てる (39 §3。実際に 1 本捕まえた)。
   36 §9g の 3 規則 (隣接ペア / 反復 3 以上 / 中央値・平均・分布を並べる) に続く 4 つ目。
3. **48 スロットは切り替え直後にページイン費用を払う** (6.78GB 常駐)。
   スイープの前に暖機を 1 本入れる (39 §3)。
4. **温度の既定 (temp 1.0 / top-k 64 / top-p 0.95) がそのまま運用点である。**
   `--temperature 0` を明示すると運用点から外れる。36 §11 はこれで n=1 のまま
   運用点でない条件を測っていた。
5. **macOS の `/bin/bash` は 3.2 で連想配列が無い** (`declare -A` が落ちる)。
6. **`sleep` を Bash ツールのコマンドに直接書くとタスクごと落ちる。**
   待つならスクリプト内に入れてバックグラウンドで投げる。
7. **生ログは必ずファイルにリダイレクトし、`bench/mtpNN/` にコピーしてコミットする**
   (`bench/logs/` は gitignore。`*.log` も gitignore なので `git add -f` が要る)。
8. **`[prefill gpu]` と `[prefill host]` は `tail` と `head` を両方持つ (意味は別物)。**
   同じ辞書に入れると先に読んだほうが上書きされる (41 §7-8 で実際に踏んだ)。
9. **計器は無料ではない。**`TF_PREFILL_HOST_PROFILE=1` + `TF_PREFILL_GPU_PROFILE=1` は
   decode を **1.9〜2.3% 遅くする** (41 §7-1)。速度比を取る run には立てない。
10. **カテゴリ軸を裏口から戻さない。**38 §3 が `register` を `2^H` に置き換え、
    41 §1a が「その `2^H` すら入力ではない」を出した。それでも「主軸となる register を
    決める」という形で 1 度戻しかけた。**選べるのはプロンプトであって、
    出てくるトークン列ではない。**
11. **条件を跨いで数字を突き合わせない。**41 の絶対 t/s は
    thinking off / CLI / 計器 ON。ゴールの `tg ≥ 30` は Reasoning ON / Server。
    **並べた瞬間に嘘になる。**22 §1 が「導出を成績表に載せた」と呼んだ失敗の別の形である。
    **2026-08-20 に 2 度目を踏みかけた** — 39 §2 の「最適 bs=2」(48 スロット / temp 1.0 /
    192 tok) を採点条件 (32 スロット / temp 0 / 生成長無制限) に持ち込もうとした (42 §2d)。
12. **「運用点」を勝手に決めない。**運用は **32 スロット**である
    (`Scripts/demo/serve.py:62`)。39/41 が 48 で測ったのはトレンドのためなのに、
    40 の初版はそれを「運用点」と呼び替えていた。**要件は serve.py とユーザーが持つ。**
13. **反復は画像 (セル) を増やす代わりにならない。**temp 0 では同一画像の run は
    決定論的で、反復が測るのは装置の再現幅だけである。**中央値の n を増やすのは
    セルを増やすこと。**反復は目盛りとドリフト検定に使う (42 §2e)。
14. **e2e 壁時計比は生成長の分岐に開いている** (42 §4)。temp 0 でも on/off が
    文字まで一致するのは 18 本中 4 本で、**セル 1 個の比は相関 −0.910 で長さに従う。**
    **中央値は頑健だが、セル 1 個の比を本文で引いてはいけない。**
15. **プローブが自分で機械を汚す。**48 のゲートは run をまたいで比が
    1.16 → 1.31 と単調に悪化した。犯人は同じプロセスの前半で走る
    `MAP_PRIVATE` の腕で、1 trial ごとに 430 MB を anonymous にして
    圧縮と swap を溜めていた。**判定に使う数字は、汚す腕と同じプロセスで取らない**
    (`--mmap-probe-gate-only`)。
16. **`madvise(MADV_DONTNEED)` はページキャッシュを落とさない。**落ちるのは
    このタスクの PTE だけで、張り直すと全ページが戻る。落とすのは
    **`msync(..., MS_INVALIDATE)`**。ただし**同じファイルに他のマッピングが
    開いていると 0 を返して落とさない**ので、測る側を張る前に行い、
    `mincore` で落ちたことを確認して retry する (48 §6)。
17. **閾値は、フォールト費用がゼロの腕でも通るか確かめてから置く。**
    48 §13 は PASS を「in-band 1.0 ms 以下」に置いたが、冷たいページが 1 枚も
    無い同じコマンドバッファが **1.16〜1.21 ms** 掛かる (49 §6)。
    根拠にした「カーネル 0.25 ms」は **1,200 パスを 1 本に積んで償却した値**で、
    コマンドバッファ 1 本ぶんの固定費が入っていなかった。
    **床は同じ試行の中で測る** (49 §0 の `warm`)。
18. **「1 個あたり」に割る前に、その軸を振る。**48 §11/§12 は
    コマンドバッファ 1 本の費用をページ数で割って µs/ページと呼び、
    production のページ/tok に掛けた。**振ったら平らだった** (49 §4) —
    ページ 8 倍で 0.06→0.12 ms。**単価ではなく固定費**だったので、
    掛け算そのものが成り立たない。
19. **カーネルの計器は production の群サイズで走らせる。**`--group-size` の既定は
    **64** で、production は **32** である (`ArchConfig.gemma4_26B_A4B`)。
    43 の 1 回目は 64 で走り、末尾の群数が半分になって別の話を測っていた。

20. **プローブハーネスの単価を production の見積もりに掛けない。**
    50 §4 は「全部コピーだった場合の費用 `C`」に 48 §3 の暖かい `pread`
    (8 エキスパートを 8 並列、21.3〜32.0 GB/s) を使い、そこから
    「D の賞金は壁時計の約 2%」を出した。**実装して測ったら 7 倍以上あった**
    (51 §2)。production の並列度は 1.2 ミス/層で、同じ `pread` でも
    8.4 GB/s しか出ていない。**50 §6 は「`C` はハーネスの値である」と自分で
    挙げていたが、符号を「`C` が小さければ賞金はさらに小さくなる」と逆に読んだ。**
    18 と同じ形 (振っていない軸を単価にした) の 2 度目である。

21. **「両腕で同一だから比較の外にある」は、片腕がそれを別の手段で
    代替していないか確かめてから言う。**51 §0 は「`F_RDADVISE` は既存経路が
    そのまま出す。両腕で同一なので比較の外にある」と置いた。**prompt prefill では
    両腕とも 0 本**で「同一」は正しいのに、**pread だけが `concurrentPerform`
    7.58 並列で深度を代替していた** (52 §3)。**同一なのは入力であって効果ではない。**
22. **腕を交互に並べると、片腕が相手のページキャッシュを踏む。**ABBA
    (`pread mmap mmap pread`) では mmap の 1 本目が必ず pread の直後に来る。
    pread は私有スロットに 3.2 GB (64 スロットなら 7.7 GB) を取って相手の
    ファイルバックを追い出す。**64 スロットでは mmap の prefill が 2 倍**に見えた
    (52 §2)。**15 と同じ形の 3 度目**で、今度は製品経路である。
    **腕ごとにブロックにして、各ブロックの 1 本目を捨てる。**
23. **`pread` の腕は連続実行で劣化する。**prefill io が 10 run で **+14.7%**
    (0.746 → 0.856 s、40 §4-2 の 5% 規則を超える)。run ごとに 3.3 GB の rss を
    積むためで、**mmap の腕は +1.3% で平ら** (52 §2a)。
    **pread の絶対値は上振れしうる。**

## 5. 揃っていない実測どうしの食い違い (**未確認**、揃えるために数字を動かしていない)

1. **計器と本番の `c` が 2.2〜2.7 倍離れている** (costProbe 0.343 対 本番 0.128〜0.155)。
   thinking を外しても消えなかった。**N5 の主題。** (36 §13-2)
2. **32 スロットの投機比 (A 1.355 / B 1.424) が 48 の比 (1.311 / 1.376) より
   両画像とも高い**のに、`c` は 48 のほうが低い。32 側が n=1 で確定していない。 (39 §6-2)
3. **同じ計算の壁時計比が夜をまたぐと 8.6% 動く。**産出トークン・受理・hit は
   ビット単位で再現するのに採点量だけが動く。 (36 §9c(b))
4. **`code` bs=6 の α (0.509) が `ja_prose` bs=6 (0.399) より高い。**
   bs 以外に効く軸が残っている。 (36 §16 末尾)
5. **参照実装との比較は片側が未定義。**llama.cpp の 26.9 ms/tok は
   `llama-bench` の合成列で register が定義されていない。 (39 §4)
6. **48 スロットが 32 スロットより遅く出た** (ja_prose、42.26 対 41.00 ms/tok)。
   ただし 32 を後に回しており、ドリフト (+0.85%) と交絡している。**ABBA が要る。**
   39 §6-3 の符号の不一致と同じ場所である。 (41 §7-2)
7. **`busy` が register でわずかに動く** (+0.5〜0.9 ms/tok)。動くのは `shared` と
   `moe` だけで `attn`/`head` は動かない。並行 pread との競合か、タイル数か。 (41 §7-3)

## 6. 計器と一次資料の在処

| | |
| --- | --- |
| スイープ | `bench/mtp_cd_sweep.py` (温度・クールダウン 20s の既定を持つ) |
| 採点条件 A/B | → 下の「採点 v2」。暖機 / クールダウン / ドリフト検定 / 共変量を持つ |
| 費用曲線 | `.build/release/TurboFieldfareKernelCheck --verify-cost-only` |
| ルーティング | `TurboFieldfareCLI --dump-expert-trace` + `bench/mtp36/an3.py` (`2^H`) |
| ブロック台帳 | `TF_PREFILL_HOST_PROFILE=1` |
| GPU の段別 | `TF_PREFILL_GPU_PROFILE=1` (`=2` で層前半を 9 分割、**帰属専用**) |
| **採点 v2** | `bench/mtp_goal_ab.py` (引数なしで 18 ペア / 約 40 分)。台帳 `bench/mtp_goal_v2_images.tsv` |
| **rows カーネル** | `--moe-rows-bench` (production 形状)、**`--moe-rows-shape-sweep`** (還元次元を振る。`--group-size 32 --moe-rows-r <1..8>`) |
| **D の先読み** | **`TF_EXPERT_MMAP_ADVISE=1`** (`MmapExpertMapping.adviseMisses`。既定 off。**両腕に出る** — 片腕だけに出すと advise の取り分を D に数える)。ドライバは `bench/mtp52_{advise,pread_advise,single_arm,slot_sweep}.sh` |
| **D の試作 (製品経路)** | **`TF_EXPERT_MMAP=1`** (`MmapExpertMapping.swift`。既定 off)。ドライバは **`bench/mtp51_mmap_ab.py`** (ABBA、ミス数と生成文 sha256 を毎 run 検定。`--draft-block-size` で MTP 条件) |
| **mmap residency** | **`--mmap-residency-probe`** (`--mmap-probe-trials` / `--mmap-probe-repeats` / **`--mmap-probe-gate-only`** / `--mmap-probe-model`)。P-1〜P-4 を 1 本で回す |
| **重みの形式** | **`--bpw-probe`** (形状を固定して affine / sym / sym8 / sym9 を振る。`--group-size 32`)。`bench/check_bias_identity.py <model.gturbo>` が格子側を検定する |
| **スキームの入れ替え** | `TF_FORCE_AFFINE_SCHEME=sym\|affine` (**計器であって設定ではない**)。データを固定して導出だけ差し替える。45 §3a の切り分けはこれで付いた |
| 集計 | `bench/mtp38_analyze.py` (register)、`bench/mtp39_analyze.py` (48 スロット)、`bench/mtp40{,b,c}_analyze.py` (N2)、`bench/mtp42_analyze.py` (N3) |
| N2 のドライバ | `bench/mtp40_n2{,b,c,d}_driver.sh` (bs=0 / bs=2,3 / 細分 / bs=4) |
| 一次資料 | **`bench/mtp52/` (D の prefill)**、`bench/mtp51/` (D の P-6)、`bench/mtp49/` (D の賞金)、`bench/mtp36/` (M9-A)、`bench/mtp38/` (register 検定)、`bench/mtp39/` (N1)、`bench/mtp40{,b,c,d}/` (N2)、`bench/mtp42/` (N3)、`bench/mtp43/` (P2)、`bench/mtp44/` (W1)、`bench/mtp45/` (W2)、**`bench/mtp48/` (D のプローブ)** |
