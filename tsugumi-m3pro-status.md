# Tsugumi / M3 Pro 現状まとめ

作成: 2026-08-16
対象: he-be/tsugumi (Apache 2.0, Swift + Metal, Gemma 4 26B-A4B)
目的: 汎用性を捨てて、この M3 Pro 1 台に特化した改造をかけるための入力

この文書の書き方のルール:

- **実測** = 手元で数字を取ったもの。条件を併記する。
- **導出** = 実測から計算したもの。誤差要因を併記する。
- **未確認** = 根拠を持っていないもの。断定しない。

---

## 1. 実機

| 項目 | 値 | 出典 |
| --- | --- | --- |
| チップ | Apple M3 Pro (CPU 12 コア / GPU 18 コア) | 実測 |
| メモリ | 18 GB (`memory_pressure` 報告 19,327,352,832 バイト) | 実測 |
| OS | macOS 15.7.5 (24G624) | 実測 |
| GPU family | apple9 = true / apple10 = false | 実測 |
| シェーダ | MSL 3.2 でコンパイル、44 関数、テンソルカーネルなし | 実測 |
| リポジトリ | `/Users/mh/LLM/tsugumi` | 実測 |
| モデル | `scratch/gemma4.moepack` (インストール済み) | 実測 |

macOS 15 で動くのは `103bfbc Lower the deployment target to macOS 15` による。
上流の要求は macOS 26 / Metal 4 なので、**この構成は上流の想定外**。
上流のベンチ値と比較するときは常にこれを念頭に置く。

---

## 2. 実測データ

### 2-1. Mac アプリ (計装が一番厚い)

設定: Cache 32 LFU / prefill 128 / FP16 KV / RDADVISE off / full SHA-256

| 項目 | 値 |
| --- | ---: |
| prompt トークン | 526 |
| output トークン | 566 |
| prefill | 10.85 s |
| prefill rate | 48.5 tok/s |
| first token wait | 1 ms |
| TTFT | 10.85 s |
| decode duration | 23.72 s |
| decode rate | 23.9 tok/s |
| peak memory | 4.77 GB |
| I/O / token | 12.5 ms |
| cb1 / token | 0.7 ms |
| cb2 / token | 0.3 ms |
| head / token | 3.2 ms |
| prefill mode | chunked -> chunked |
| completeness | complete |

1 回のみ。ウォームアップなし。中央値ではない。

### 2-2. CLI / 冷温と prefill on-off (616 tok, `--max-new 128`)

条件: `--expert-cache-slots 32 --expert-cache-policy lfu --rdadvise off`

| 条件 | decode tok/s |
| --- | ---: |
| cold (`sudo purge` 直後) prefill on | 23.108 |
| warm prefill on | 22.562 / 22.402 |
| prefill on (連続 4 回) | 22.389 / 22.179 / 22.193 / 22.320 |
| prefill off (連続 4 回) | 24.795 / 24.816 / 24.874 / 24.875 |

条件内のばらつきは on で 0.9%、off で 0.3%。
セッション全体で 23.108 → 22.2 へ単調に近い低下がある (約 4%)。

### 2-3. CLI / prefill 壁時計 (518 tok, `--max-new 1`, 4 条件インターリーブ × 3, クールダウン 20 s)

`real` は `/usr/bin/time -p`。`decode` は footer 由来で `--max-new 1` のため 0.00 s。

| 条件 | real (3 回) | 中央値 |
| --- | --- | ---: |
| ロード単体 (15 tok プロンプト) | 8.61 / 8.56 / 8.55 | **8.56** |
| prefill on, chunk 128 | 16.88 / 15.82 / 15.84 | **15.84** |
| prefill on, chunk 32 | 17.04 / 17.09 / 17.40 | **17.09** |
| prefill on, chunk 64 | 21.51 / 18.49 / 18.85 | **18.85** |
| prefill off | 26.07 / 26.13 / 25.73 | **26.07** |

---

## 3. 導出値とその信頼度

ロード時間 8.56 s を定数として引いた prefill 換算:

| 条件 | prefill 換算 | tok/s 換算 |
| --- | ---: | ---: |
| on, chunk 128 | 7.28 s | 71.2 |
| on, chunk 32 | 8.53 s | 60.7 |
| on, chunk 64 | 10.29 s | 50.3 |
| off | 17.51 s | 29.6 |

**この引き算の誤差要因 (無視できない):**

- ロード時間は別プロンプト (15 tok) の測定値を定数扱いしている。
  そのプロンプト自身の prefill 時間が入っているので、**上限側にずれている**。
- 引かれる値 (8.56) と結果 (7.28) が同オーダー。ロードのばらつきがそのまま
  prefill 推定に乗る。
- したがって **2 倍以上の差は読めるが、10〜20% の差は判定できない**。
  chunk 32 と 64 の逆転はこの精度では意味を持たない。

---

## 4. 現時点で言えること

### 4-1. チャンク化 prefill は機能している

on/off で 2.4 倍。この差は引き算の誤差より十分大きい。
「チャンク化が実質何も買えていない」という当初の仮説は棄却。

`off` の 29.6 tok/s は decode の 23 tok/s に近い。`--prefill off` が
1 トークンずつ処理する経路である、という読みと整合する (未確認)。

### 4-2. ファイルキャッシュの温冷は decode に影響しない

`sudo purge` 直後のほうがむしろ速い。ページキャッシュに依存した高速化は
起きていない。エキスパート読み出しが毎回 SSD に降りているためと思われるが、
**機序は未確認**。

### 4-3. `--prefill off` にすると decode が約 12% 速くなる

22.2 → 24.8 tok/s。条件内ばらつき 1% 以下、かつドリフトで不利な後半に測って
なおこの差なので、ノイズではない。

作業仮説: チャンク化 prefill が 32 スロットの LFU を prefill の
アクセス分布で埋め、decode の top-8 パターンに合わないキャッシュ状態で
decode に入っている (カウンタ汚染)。**未検証。** `--expert-cache-policy lru`
との比較で切り分けられるが、まだ回していない。

### 4-4. 測定にサーマルドリフトが乗る

連続実行で decode が約 4% 低下する。条件を順番に並べる測定はできない。
インターリーブ + クールダウンが必須。

### 4-5. prefill と decode の速度差が小さい

on128 換算で prefill 71 tok/s、decode 23 tok/s。**3 倍程度**。
128 トークンをまとめて流している以上、もっと開いてよさそうに見えるが、
「どれくらい開くのが正常か」の基準を持っていないので、これが異常かどうかは
**判断できない**。上流の M2 / M5 のベンチには prefill rate が載っていない。

---

## 5. 分かっていないこと

改造の前に潰すか、潰さないと決めるかを選ぶ項目。

1. **prefill が I/O 律速か計算律速か。**
   これが最大の未解決点。on/off で 2.4 倍出たことは「チャンク化が何かを償却した」
   ことを示すだけで、残る 7.3 s の内訳を何も言っていない。
   アプリは decode の I/O ms/token を出すが、**prefill の I/O 内訳は出していない**。

2. **CLI と Mac アプリで prefill が 33% 違う理由。**
   CLI 換算 7.3 s (518 tok) 対 アプリ 10.85 s (526 tok)。設定は揃えたつもり。
   候補: 引き算の誤差 / decode service との IPC / アプリ側の計測範囲の違い。
   **どれも未確認。** どちらの数字を基準にするか決まらないと改造効果が測れない。

3. **`full SHA-256` が何を検証しているか。**
   アプリの設定表示に出ている。インストール時のマニフェスト検証だけなのか、
   読み出しごとに走るのかを読んでいない。後者なら I/O 12.5 ms/token の一部が
   ハッシュ計算の可能性がある。**未確認。ソースを読めば済む。**

4. **1 層あたりのエキスパート数。**
   README は top-8 ルーティング、既定 16 スロット LFU としか書いていない。
   総数が分からないと、32 スロットが妥当なのか焼け石なのかを議論できない。
   `docs/SYSTEM_DESIGN.md` で確認できるはず。

5. **キャッシュ上限 32 の根拠。**
   CLI は 8/16/24/32 しか受け付けない。この上限が構造的な制約なのか、
   8 GB Mac を想定した安全側の設定なのかを読んでいない。
   peak 4.77 GB / 18 GB で 13 GB 使っていないので、**上限が任意なら伸ばす余地がある**。

6. **チャンク上限 128 の根拠。** 同上。32/64/128 のみ。

7. **`--rdadvise` の効果。** off 以外を一度も測っていない。
   4 値 (off/default/bounded/adaptive) あり、I/O 経路に直接効くはずの唯一のノブ。

8. **MPP 行列カーネル欠落の実影響。**
   macOS 15 では `mpp_prefill_affine_threadgroup_f16` がライブラリに存在せず
   `.unavailable` になる。約 12% という数字は上流 PF-12 の**別機体の値**で、
   この M3 Pro では**一度も測っていない**。

9. **TensorOps attention は最初から無関係。**
   `device.supportsFamily(.apple10)` でゲートされており、この機体は apple9 まで。
   macOS 15 に落としたことによる損失ではない。(TODO 記載、コード未読なので要確認)

10. **`.q` 投影が `.repeatedGEMV` である選択の妥当性。**
    `PrefillProjectionDispatchPolicy.selectedDispatch` の選択は MPP 導入以前の
    測定に基づく、と TODO にある。macOS 15 常用なら `.qmm` のほうが速い可能性。
    **未測定。**

---

## 6. 計装が足りない

改造の効果を測る土台がない。ここを先に直さないと、以降の変更は全部
引き算の誤差に埋もれる。

- **CLI の footer に prefill 秒数と TTFT がない。**
  現在の footer:
  `[stop=maxTokens prefill=616tok new=128tok decode=5.54s tok/s=23.108]`
  `prefill=` はトークン数であって時間ではない。アプリは秒数を表示しているので
  ランタイムは値を持っている。footer 生成箇所に追加するだけのはず (未確認)。

  ```bash
  grep -rn 'tok/s=' Sources/
  ```

- **prefill の I/O 内訳がどこにも出ない。** decode には cb1 / io / cb2 の
  フェーズ計装があるが、prefill 側に同等のものがあるかは未確認。
  なければ追加が必要。5-1 を解くにはこれが要る。

- **ロード時間が計測から分離できない。** CLI 実行時間の 8.56 s、
  つまり 518 tok 測定の半分以上がロード。footer にロード完了時刻が出れば
  引き算が不要になる。

---

## 7. 改造候補

根拠の強さ順。上ほど確実、下ほど当て推量。

### A. 計装の追加 (根拠: 測定できていないという事実)

footer に prefill 秒数 / TTFT / ロード時間を追加。prefill に I/O フェーズ計装を追加。
**他のすべての前提。最初にやる。**

### B. prefill と decode でキャッシュを分離、または prefill 後にカウンタをリセット
(根拠: 4-3 の 12%、ただし機序は仮説)

まず `--expert-cache-policy lru` で仮説を検証する。改造はその後。
汎用性を捨てられるなら、prefill 用と decode 用でスロットを分ける、
prefill 中の参照をカウンタに載せない、といった選択肢がある。

### C. キャッシュスロット上限の引き上げ (根拠: 13 GB 未使用。上限の性質は未確認)

上限 32 が任意なら、18 GB 前提で大幅に増やせる可能性がある。
5-4 (1 層あたりのエキスパート数) と 5-5 (上限の根拠) を読んでから。
**全エキスパートが常駐できる規模なのかどうかで話が根本的に変わる。**

### D. チャンクサイズ上限の引き上げ (根拠: 弱い)

on128 が最速だったが、32 と 64 が逆転しており単調ではない。
引き算の精度では傾向を読めていない。**A の後に測り直してから判断する。**

### E. `--rdadvise` の 4 値を測る (根拠: 未測定なので不明。コストは低い)

I/O 経路に直接効く唯一のノブ。prefill と decode で最適値が割れる可能性がある。
アプリの注記は decode についてしか言っていない。

### F. `.q` 投影を `.qmm` に変える (根拠: TODO の推測のみ)

macOS 15 常用前提での再選択。A の後に測る。

### G. macOS 26 に上げる (根拠: 別機体の 12%)

MPP が戻る。この機体での効果は未測定。金銭コストはない。
ただし**上げると macOS 15 特化の最適化 (F など) の前提が変わる**ので、
上げるなら先に上げて、その状態を基準に改造を始めたほうがよい。
逆に macOS 15 に留まると決めるなら、そう決めてから始める。
**この分岐を先に決める必要がある。**

---

## 8. 手をつける順序 (案)

1. 5-3 / 5-4 / 5-5 / 5-6 をソースで確認する (読むだけ、測定不要)
2. G の分岐を決める (macOS 15 に留まるか、26 に上げるか)
3. A を実装する
4. A の計装で 2-1 / 2-3 を測り直し、5-2 (CLI とアプリの 33% 差) を潰す
5. B の仮説検証 (lru) と E を測る
6. ここまでの結果を見て C / D / F を判断する

---

## 9. 測定手順のメモ

- Mac アプリ / decode service / CLI / server は同時に 1 つだけ。
- サーマルドリフトがあるので、条件はインターリーブし、実行間にクールダウンを入れる。
- `swift run` ではなく `.build/release/` の実体を直接叩く (SwiftPM の依存解決が
  計測に乗る)。
- 再現性のため `--temperature 0 --seed 1 --max-context 4096` を固定。
- CLI の既定スロットは 16。アプリ実測 (32) と比較するなら明示指定が必要。
- 現行のベンチスクリプトは `bench.sh`。結果は `bench/results.tsv` に追記される。

## 10. CLI が受け付ける値 (`--help` より)

| フラグ | 値 | 既定 |
| --- | --- | --- |
| `--expert-cache-slots` | 8 / 16 / 24 / 32 | 16 |
| `--expert-cache-policy` | lfu / lru | lfu |
| `--prefill` | on / off (on は 16 スロット以上必須) | on |
| `--prefill-chunk-tokens` | 32 / 64 / 128 | 128 |
| `--rdadvise` | off / default / bounded / adaptive | off |
| `--max-new` | int | 1024 |
| `--max-context` | int | 4096 |
