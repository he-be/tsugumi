# PLAN — M3 Pro 18GB / macOS 15 専用化

作成: 2026-08-16 / 更新: 2026-08-16 (Phase 0-2 実施後)
入力: `tsugumi-m3pro-status.md` (実測), `TODO.md` (macOS 15 フォールバックの分析)
方針: 汎用性を捨てて、この 1 台 (M3 Pro / 18GB / macOS 15.7.5) の常用機にする

表記は status ドキュメントと同じ:
**実測** = 手元で数字を取った / **導出** = 実測からの計算 / **未確認** = 根拠なし

---

## 進捗

| Phase | 状態 | 結果 |
| --- | --- | --- |
| 0 計装 + トレース | **完了** | 出口条件クリア。偏りは想定よりはるかに強い |
| 1 起動時間 | **完了** | 層 SHA-256 の 5.4 s を削減 |
| 2 スロット上限 | **完了** | 既定 16 → **64** (96 ではない、下記 §2 参照) |
| 3 先読み | **目的を再定義** | ヒット率目的では不要になった (§Phase 3) |
| 4 チャンク上限 | 未着手 | 相対的な重要度は上がった |
| 5 文脈長 | 未着手 | 次にやるならここ |
| 6 投機的 | **不要と判断** | 96 でも 128 でもヒット率が頭打ち |

**数字と詳細は `RESULTS.md`。** この PLAN 側は、当初の想定が実測でどう
覆ったかだけを各節に追記してある。

### 目標に対する到達点 (**実測**)

haiku.json / 384 tok 生成 / インターリーブ 3 回中央値。
変更前 = 16 スロット + `full-sha256`、変更後 = 64 スロット + `trusted-install`。

| | 変更前 | 変更後 |
| --- | ---: | ---: |
| decode | 22.96 tok/s | **31.02 tok/s** (+35%) |
| TTFT | 12.58 s | **9.63 s** (−2.95 s) |
| peak | 2.23 GB | **7.11 GB** |

**「12 GB で 30 tok/s 台」は 7.1 GB で達成。** 予算は 4.9 GB 余った。

---

## 0. 先に決めた分岐

### 0-1. macOS 15 に留まる (決定)

status 7-G の分岐は **15 に留まる**で確定。以後の最適化はすべて MSL 3.2
フォールバック経路 (MPP 行列カーネルなし) を前提に測る。

失うもの: prefill の投影で MPP パスが使えない (上流 PF-12 の別機体で約 12%)。
TensorOps attention は `device.supportsFamily(.apple10)` ゲートで、この機体は
apple9 止まり (**実測**) なので元から無関係。**TODO.md の 9 番は確認済み。**

### 0-2. メモリ目標

| 用途 | 予算 |
| --- | ---: |
| LLM (このプロセス全体) | 12 GB |
| OS + その他アプリ | 6 GB |
| 合計 | 18 GB (`hw.memsize` = 19,327,352,832) |

12 GB という数字には裏付けがある。**実測**で

```
Metal maxBufferLength               =  9.66 GB   (単一 MTLBuffer の上限)
Metal recommendedMaxWorkingSetSize  = 12.88 GB   (常駐させてよい総量の推奨値)
```

つまり Metal 自身がこの機体で「12.88 GB まで」と言っている。目標 12 GB は
推奨作業セットのすぐ内側で、偶然ではなく妥当な線。

### 0-3. 比較対象

llama.cpp で同じ Gemma 4 26B-A4B を全部 (約 15 GB) 載せると 30-40 tok/s。
ただしメモリがギリギリで常用不可。**目標は「12 GB で 30 tok/s 台」。**

---

## 1. ソースを読んで確定した事実

status 5 節の未確認項目のうち、コードを読むだけで潰せるものを潰した。

### 1-1. モデル形状 (manifest.json より、**実測**)

| 項目 | 値 |
| --- | ---: |
| numLayers | 30 |
| numExperts (1 層あたり) | **128** |
| topKExperts | 8 |
| hiddenSize | 2816 |
| moeIntermediateSize | 704 |
| headDim / numKVHeads | 256 / 8 |
| fullHeadDim / numFullKVHeads | 512 / 2 |
| slidingWindow | 1024 |
| full attention 層 | 5 / 30 |
| vocabSize | 262144 |

→ **status 5-4 解決。1 層 128 エキスパートのうち 8 を使う。**
32 スロットは 25%、16 スロットは 12.5% しか常駐していない。

### 1-2. ファイルレイアウト (**実測**)

```
expertStride     = 3,358,720 B = 3.203 MiB = 16 KiB ページ × 205 (ページ境界ちょうど)
layer_NN.bin     =   429,916,160 B = 410 MiB = 128 × expertStride
packed_experts/  = 30 × 410 MiB   = 12.01 GiB = 12.90 GB
model_weights.bin=  1,353,771,068 B = 1.354 GB (常駐コア)
合計                              = 14.25 GB
```

expertStride がページ境界ちょうど、というのは後の選択肢 (mmap 化) に効く。

### 1-3. メモリ消費は完全にスロット数の一次関数 (**導出**、実測と一致)

`PreadExpertStreamer` は**層ごとに独立**して `slotCount` 個のスロットを持つ
(`Model.swift:302-319`)。スロットは `posix_memalign(2MiB)` の匿名メモリを
`makeBuffer(bytesNoCopy:)` で MTLBuffer 化したもの
(`PreadExpertStreamer.swift:141-160`)。

```
expert メモリ = 30 層 × slots × 3.3587 MB = slots × 100.8 MB
総メモリ      ≒ 1.354 GB (コア) + slots × 100.8 MB + KV/scratch
```

検算: slots=32 → 1.354 + 3.224 + 0.28 = **4.86 GB**。
アプリ実測の peak 4.77 GB と 2% 以内で一致。**式は信用してよい。**

### 1-4. スロット上限 32 は構造的制約ではない (**実測**)

```swift
// Sources/Tsugumi/Runtime/Configuration/RuntimeConfiguration.swift:23
public static let allowedExpertCacheSlots = [8, 16, 24, 32]
public static let allowedPrefillChunkTokens = [32, 64, 128]
public static let minimumExpertCacheSlotsForChunkedPrefill = 16
```

ただの許可リスト。`PreadExpertStreamer` は `slotCount > 0` しか要求しない。
上流が 8 GB M2 を基準にしているだけで、**引き上げは配列の書き換えで通る。**

→ **status 5-5 解決。上限は任意。伸ばす余地がそのままある。**

真の下限側の制約は 2 つだけ:
- chunked prefill は 16 スロット以上 (上の定数)
- prefill タイルの live experts は 16 以下 (`PrefillGroupedRoutedMoE.swift:229`)

上限側の制約はコードに存在しない。

### 1-5. チャンク上限 128 も 2 箇所の定数 (**実測**)

```swift
// PrefillRuntimeConfig.swift:171
public static let maxChunkTokens = 128
// PrefillChunkScratch.swift:16
self.chunkTokens = max(1, min(chunkTokens, 128))
```

scratch は `chunkTokens` に線形 (`routePartialElements = chunk × topK × hiddenSize`)。
chunk 128 で routePartials は約 5.8 MB。512 にしても 23 MB で、12 GB 予算では誤差。

→ **status 5-6 解決。上限は任意。**

### 1-6. full SHA-256 は「層ファイル初回オープン時に 1 回」(**実測**)

`Model.swift:281-299`。読み出しごとには走らない。
→ **status 5-3 解決。I/O 12.5 ms/token にハッシュは含まれない。**

ただし別の問題が見えた。層は**遅延オープン**なので、30 層 12.9 GB のハッシュは
**最初の forward pass の中で**走る。CommonCrypto は HW 加速で **実測 2.7 GB/s**
(openssl で 410 MB を 0.15 s user)。

```
12.90 GB / 2.7 GB/s ≒ 4.8 s (CPU) + SSD から 12.9 GB の読み出し
```

status 2-3 の「ロード単体 8.56 s」の大半はこれ、というのが作業仮説
(**未検証、Phase 0 の計装で確定する**)。

回避策は**既に実装済み**: `ModelIntegrityPolicy.sizeCheckTrustedReceipt` と
`verified-install.json` (インストール済み) がある。アプリは設定で切替可能。
**CLI とサーバは `.fullSha256` 決め打ちでフラグがない**
(`CLI/Run.swift:68`, `ServerInference.swift:409`)。

### 1-7. KV は 4K でも 32K でも安い (**導出**)

```
sliding 層 (25):  1024 tok × 2(K,V) × 8 heads × 256 dim × 2 B = 8 MiB/層 → 200 MiB (文脈長に非依存)
full 層 (5):      ctx  × 2(K,V) × 2 heads × 512 dim × 2 B = 4 KiB/tok/層
                   4K →  80 MiB   /  32K → 640 MiB
```

| 文脈長 | KV 合計 |
| ---: | ---: |
| 4K | 280 MiB |
| 16K | 520 MiB |
| 32K | 840 MiB |

**12 GB 予算なら 32K 文脈がほぼタダで載る。** これは想定していなかった収穫。

### 1-8. エキスパートのバインドは「バッファ先頭」固定 (**実測**)

`expertCachePlanBuffers` は `(buffer, offset: 0, size)` を返し
(`PreadExpertStreamer.swift:307`)、MoE の引数バッファも
`routedArgEncoder.setBuffer(blob, offset: 0, index:)` (`MoE.swift:306`)。
`TensorView.offset` は運ばれているが、routed expert では常に 0。

→ 「1 層 1 個の大きいバッファ + エキスパートごとのオフセット」に変える改造
(mmap 全常駐など) をやるなら、**ここのオフセット配管が必須の前提**になる。
`makeRoutedArgumentBuffer` と `encodeRoutedPersistentPhase1/2` の 3 箇所。

### 1-9. キャッシュ計画のホットパスはスロット数に対して線形〜O(S log S) (**実測**)

`makeExpertCachePlan` (`PreadExpertStreamer.swift:217-268`) は 1 層 1 トークンごとに

- ヒット探索: エキスパート 8 個 × 全スロット線形走査 = 8 × S
- 追い出し候補: 全スロットを filter して **sorted** = O(S log S)、**ミスが 0 でも実行される**

S=32 では無視できるが、S=96 だと 30 層 × (768 + 約 630 比較) / token。
**スロットを増やす改造とセットで直さないと、削った I/O を CPU で食い潰す。**

---

## 2. メモリ予算とスロット数の対応表 (**導出**)

`総メモリ ≒ 1.354 GB + slots × 0.1008 GB + KV(0.28 GB @4K)`

| slots | 常駐率 | expert メモリ | 総メモリ | 判定 |
| ---: | ---: | ---: | ---: | --- |
| 16 | 12.5% | 1.61 GB | 3.2 GB | 上流既定 |
| 32 | 25% | 3.22 GB | 4.86 GB | **現状** (実測 4.77 GB) |
| 48 | 37.5% | 4.84 GB | 6.5 GB | |
| 64 | 50% | 6.45 GB | 8.1 GB | |
| 80 | 62.5% | 8.06 GB | 9.7 GB | |
| **96** | **75%** | **9.68 GB** | **11.3 GB** | **主目標** |
| 104 | 81% | 10.5 GB | 12.1 GB | 予算ちょうど |
| 112 | 87.5% | 11.3 GB | 12.9 GB | 推奨作業セット 12.88 GB に接触 |
| 128 | 100% | 12.9 GB | 14.5 GB | **不可** (推奨超過、llama.cpp 相当) |

**96 スロットを主目標**、112 を上限側の実験値とする。
128 (全常駐) は Metal の推奨作業セットを超えるので、そもそもこの機体では
「全部載せ」は選べない。llama.cpp が 15 GB でギリギリなのと同じ壁。

> **実施後の訂正: 主目標は 96 ではなく 64。**
>
> メモリ側の予測は正しかった (112 = 12.96 GB で実際に推奨作業セット 12.88 GB を
> 超え、Phase 2 のメモリガードが弾いた)。外したのは性能側で、
> **64 スロットで tok/s が頭打ちになる** (**実測**)。
>
> | slots | tok/s | io ms/tok | decode hit | peak |
> | ---: | ---: | ---: | ---: | ---: |
> | 32 | 27.9 | 7.9 | 91.7% | 3.9 GB |
> | 48 | 29.8 | 3.9 | 97.8% | 5.5 GB |
> | **64** | **31.2** | 2.0 | 99.2% | **7.1 GB** |
> | 96 | 31.2 | 0.85 | 99.8% | 9.4 GB |
>
> 48 は 64 比 −4.5%、96 は 64 と同値。理由は §3 の訂正を参照。
> **既定は 64 にした。** 96 も 112 も許可リストには入れてあるので、
> 後から試すことはできる (112 はこの機体ではガードに弾かれる)。

---

## 3. 期待効果 (**導出**、要検証)

decode 23.9 tok/s = 41.8 ms/token。内訳 (アプリ実測):

| フェーズ | ms/token |
| --- | ---: |
| I/O | 12.5 |
| cb1 | 0.7 |
| cb2 | 0.3 |
| head | 3.2 |
| 未計装 (attention/MoE 本体ほか) | 約 25 |

I/O がゼロになれば 41.8 → 29.3 ms = **34 tok/s**。llama.cpp の 30-40 tok/s と同圏。
メモリは 11.3 GB なので目標達成。

**この試算が成立する条件は「スロットを増やすとヒット率が実際に上がること」だけ。**
それはルータのエキスパート使用分布の偏りに依存し、**未測定**。
偏りがなければ 96/128 = 75% のヒット率止まりで、I/O は 12.5 → 3.1 ms にしか
下がらない (それでも 41.8 → 32.4 ms = 30.9 tok/s なので無駄ではない)。

**Phase 0 でこの分布を測ることが、計画全体の前提。**

> **実施後の訂正: 条件は 2 つあり、片方しか成立しなかった。**
>
> **(a) ヒット率は上がる — 想定を大きく上回った。** ルータの偏りは非常に強く、
> 12.5% の常駐で 71%、50% の常駐で 96.5%、75% の常駐で 99.2% (**実測**)。
> 「偏りがなければ 75% 止まり」という悲観シナリオは杞憂だった。
> ちなみに 128 スロット (全常駐) でも 99.3% で 100% にはならない。
> 残りは初回参照の compulsory miss なので、96 超は原理的に買えるものがない。
>
> **(b) しかし引き算モデルが成立しない。** ここが誤り。
> `totalIoNanos` が測っているのは **pread の待ち時間**であって、
> その裏で GPU は shared MLP を回している
> (`RealForwardRunner.swift:1571-1595` の意図的な設計)。
> I/O を消しても、隠れていた GPU 時間が表に出てくるだけ。
>
> ```
> 16 → 96 slots:  io −16.4 ms/tok  →  ms/token は −15.0 ms  (ほぼ 1:1)
> 64 → 96 slots:  io  −1.2 ms/tok  →  ms/token は   0.0 ms  (完全に隠れる)
> ```
>
> **64 スロットから先は decode が GPU 律速。** 内訳 (64 スロット、**実測**):
> `io=2.15 cb1=0.66 cb2=0.23 head=3.24` = 計装済み 6.3 ms に対し、
> **未計装 (attention / MoE 本体) が約 26 ms。**
> 次に効くのは I/O ではなくカーネル本体。

---

## 4. フェーズ

### Phase 0 — 計装と分布の測定 (他のすべての前提) — **完了**

> 1-5 すべて実装済み。footer は既存の 1 行目を温存して下に 3 行足したので、
> `bench.sh` の既存パーサは壊れていない。
> 4 の出力は `bench/expert_sim.py` が食う。追い出し規則を写した replay で、
> **1 本のトレースから全スロット数・両ポリシーのカーブが引ける。**
> 検証: slots=16 で sim が 65.0%/miss 1260、実機 footer が 65.0%/1259 で一致。
> **出口条件クリア。**

status 6 節と同じ結論。追加で「エキスパート使用分布」を最優先で取る。

1. CLI footer に **ロード秒 / prefill 秒 / TTFT / peak RSS** を追加。
   現在 `prefill=616tok` はトークン数であって時間ではない。ランタイムは
   アプリ向けに値を持っているので、footer 生成箇所で出すだけのはず。
   → status 5-2 (CLI とアプリで prefill が 33% 違う) もこれで潰れる。
2. 層ファイルの SHA-256 秒数を個別に計上する。1-6 の仮説の検証。
3. expert cache の **hit / miss カウンタ**を footer に出す (現状ゼロ計装)。
4. `--dump-expert-trace <path>`: 層ごと・トークンごとの top-8 エキスパート ID を
   吐く。**この 1 個のトレースから、スロット数 vs ヒット率のカーブを
   オフラインで全部引ける。** 実機で 8 通り測る必要がなくなる。
5. prefill 側の I/O フェーズ計装 (decode の cb1/io/cb2 相当)。status 5-1 用。

**Phase 0 の出口条件**: 「slots=64 / 96 / 112 でヒット率が何 % になるか」を
1 回のトレースから机上で答えられること。ここで期待値が出なければ Phase 2 は中止。

### Phase 1 — 起動時間 (低リスク・効果が確実) — **完了**

> `--verification full-sha256|trusted-install` を CLI とサーバに露出した
> (`ModelIntegrityPolicy` に共通の CLI 名を持たせ、Mac アプリの語彙と揃えた)。
>
> **1-6 の作業仮説は当たっていた** (**実測**): `Model.load` 本体は 0.69 s しか
> かからず、`layerVerify=5.42s/30layers` が最初の forward pass の中で走っていた。
> 12.9 GB / 5.42 s = 2.38 GB/s で openssl の実測 2.7 GB/s と整合。
>
> 効果は **−5.4 s** (見積り約 5 s に対して妥当)。ただし SHA-256 のパスには
> 12.9 GB をページキャッシュに載せる副作用があるので、正味の TTFT 短縮は
> 約 −4.0 s。既定は `full-sha256` のまま、常用時に明示指定する。

`--verification trusted` (= `sizeCheckTrustedReceipt`) を CLI とサーバに露出する。
既存機能の露出だけで、新規ロジックはゼロ。

期待: 起動から初回トークンまで **約 5 s 短縮** (**導出**、1-6 より)。
常用するならこれが一番効く。ベンチの引き算誤差も一気に小さくなる。

`verified-install.json` の受領証が壊れていたら弾かれるので、安全性の後退は
「インストール後にファイルが書き換わっても気付かない」だけ。ローカル常用なら許容。

### Phase 2 — スロット上限の引き上げ (本命) — **完了**

> 1-4 すべて実施。ただし 4 の既定値は 96 ではなく **64** (§2 の訂正)。
>
> - 1: `allowedExpertCacheSlots = [8, 16, 24, 32, 48, 64, 80, 96, 112]`。
>   128 は §8 の却下どおり入れていない。App の Picker は予想どおり自動追従。
> - 2: `expertResidency[]` で**ミス判定を O(1)** 化 (0 なら確定ミス)、
>   `misses.isEmpty` なら追い出し候補の選抜を丸ごとスキップ、
>   要るときも `cheapestEvictableSlots` で必要な個数 (decode なら 8) だけ選ぶ。
>   **全体 sort は廃止。** 重複エキスパートのために `expertSlotHint` は
>   ヒント扱いにして、外れたときだけ従来の線形走査へ落とす。
>   **書き換え前後で hit/miss が完全一致** (`71.0% 21629/30480`) = 純粋な高速化。
> - 3: `ExpertCacheBudget` が `resident + numLayers*slots*expertStride + KV` を
>   `recommendedMaxWorkingSetSize` と比較し、`RealForwardRunner.init` で拒否。
>   **実測で 112 slots = 12.96 GB > 12.88 GB を正しく弾いた** (§2 の表と一致)。

1. `allowedExpertCacheSlots` に `48, 64, 80, 96, 112` を追加
   (`RuntimeConfiguration.swift:23`)。App の Picker
   (`InspectorView.swift:87`) はこの配列を参照しているので自動追従。
   CLI / サーバのヘルプ文言とエラーメッセージは手で直す (4 箇所)。
2. **1-9 のホットパス最適化を同時に入れる** (これが本体)。
   - `expertSlot: [Int]` (サイズ 128) の逆引きを持ち、ヒット探索を O(1) に
   - `misses.isEmpty` なら追い出し候補の sort をスキップ (早期 return)
   - sort が要る場合も、必要なのは下位 `misses.count` 個だけ。全体 sort は不要
3. **メモリガード**: 起動時に `1.354 GB + slots × 100.8 MB + KV` を計算し、
   `device.recommendedMaxWorkingSetSize` と比較して、超えるなら拒否 or 警告。
   ここを守らないと OS がスワップ/圧縮を始めて逆に遅くなる。
4. 既定値をこの機体向けに 96 へ変更 (汎用性は捨てる方針なので可)。

測定: 32 (baseline) / 64 / 96 を **インターリーブ + クールダウン**で。
サーマルドリフト約 4% があるので条件を並べる測定は不可 (status 4-4)。

### Phase 3 — キャッシュを実際に埋める — **目的を再定義**

> **「ヒット率を上げる」目的では不要になった。** 510 tok の prefill を
> 終えた時点で 64 スロットの decode ヒット率が 99.2% に達している (**実測**)。
> **prefill が事実上の先読みになっている。**
>
> 一方で「起動直後はむしろ遅い」という懸念は当たっていた。TTFT を分解すると:
>
> ```
> 16 slots, SHA なし : 約 7.2 s
> 64 slots, SHA なし :     9.63 s
>                      差 +2.4 s ← 30層×64×3.36MB = 6.4 GB の初回充填
> ```
>
> Phase 1 の −5.4 s が Phase 2 の +2.4 s を吸収して正味 −2.95 s。
> **`trusted-install` はスロット増と必ずセットで使うこと。**
>
> → **やるなら目的は「TTFT を下げる」。** 静的な頻出リストではなく、
> ロード直後のバックグラウンド充填で TTFT の +2.4 s を隠す方向。
>
> 注意: prefill 秒はセッションをまたぐと 7.6-9.4 s と振れる (ページキャッシュ
> 依存)。**スイープの内側でインターリーブした値どうしでしか比較しないこと。**

96 スロットは lazy にしか埋まらない。起動直後は全ミスで、むしろ遅い可能性がある。

- Phase 0 のトレースから **層ごとの頻出エキスパート上位 96** の静的リストを作り、
  ロード直後にバックグラウンドで先読みする。12.9 GB のうち 9.7 GB を読むので
  SSD 実測 1-2 GB/s だと **5-10 s**。サーバ常駐なら 1 回払えば済む。
- 併せて `--rdadvise` の 4 値を測る (status 7-E)。先読みの実装と競合する
  ノブなので、この段階でまとめて決める。
- status 4-3 の「`--prefill off` で decode が 12% 速い」(prefill による LFU
  カウンタ汚染の仮説) は、常駐率 75% では大部分が消えるはず。
  **Phase 2 の後に再測して、まだ残るなら prefill 中の参照をカウンタに
  載せない改造をする。** 先に `lru` との比較で切り分ける (status 7-B)。

### Phase 4 — prefill チャンク上限の引き上げ — **重要度が上がった**

> decode が 32 ms/token まで下がった結果、**TTFT の中身はほぼ全部 prefill**
> (9.6 s) になった。相対的な重要度は当初想定より高い。
> 「prefill が I/O 律速か計算律速か」は Phase 0 の計装で答えが出ていて、
> 64 スロットで prefill io = 3.1 s / prefill 全体 9.4 s なので **計算律速寄り**。
> チャンクを大きくして GPU 効率を取りに行く方が筋がよい。

`maxChunkTokens` を 256 / 512 に。scratch は線形なので実害は小さい (1-5)。
常駐率 75% だとチャンクを大きくする意味が変わる (I/O 償却 → GPU 効率)。
Phase 0 の計装で prefill が I/O 律速か計算律速か (status 5-1) が分かってから判断。

同時に `.q` 投影を `.repeatedGEMV` から `.qmm` に変える実験 (status 7-F)。
macOS 15 常用が確定したので、MPP なし前提での再選択には意味がある。

### Phase 5 — 文脈長を伸ばす (おまけ、コストが低い) — **次にやるならここ**

> 予算がさらに余った (7.1 GB なので 12 GB まで 4.9 GB)。
> `ExpertCacheBudget` が KV 込みで判定するので、伸ばしすぎたら起動時に弾かれる。

1-7 より、32K 文脈でも KV は 840 MiB。12 GB 予算なら余裕で入る。
`--max-context` の既定を 4096 から 16384 か 32768 に上げる。
sliding window 1024 の 25 層は文脈長に非依存なので、伸ばしても
attention コストの増加は full attention 5 層だけ。

### Phase 6 — 投機的 (Phase 2 の結果次第。やらない可能性が高い) — **やらない**

> 「やらない可能性が高い」が的中。**両方とも買えるものがない。**
>
> - 層ごとの非一様配分: 64 スロットで既に 99.2%、96 で 99.8%、128 (全常駐)
>   でも 99.3% 止まり。**配分を工夫して取り返せる余地が 0.8 ポイントしかない。**
>   しかもその 0.8 ポイントは tok/s に効かない (§3 の訂正)。
> - 物理再配置 + mmap: 同上。メモリ圧に対する安全性は魅力だが、
>   7.1 GB / 18 GB なら圧がかからない。工数に見合わない。
>
> 代わりにここへ回すべきなのは **decode の未計装 26 ms/token の分解**
> (attention / MoE カーネル本体)。PLAN にない項目だが、
> I/O 律速を脱した今、唯一の残りがここ。

- **層ごとに非一様なスロット配分**: 現状は全層一律。層ごとに偏りが違うなら、
  グローバル予算 (96 × 30 = 2880 スロット) を偏りに応じて配分する。
  Phase 0 のトレースで層ごとのエントロピーを見てから判断。
- **物理再配置 + mmap**: `ExpertEntry.physicalRank` が既に形式にあり
  (`PackedExpertsLayout.swift:19`)、層ファイル内でエキスパートを頻度順に
  並べ替える再パックが**形式上サポートされている**。頻出 96 個を
  ファイル先頭に連続配置すれば、その範囲を mmap して 1 個の MTLBuffer として
  ゼロコピー常駐できる (expertStride がページ境界ちょうど、1-2)。
  匿名メモリではなくファイルバックになるので、圧縮/スワップではなく
  ページキャッシュの追い出しで劣化する = メモリ圧に対して安全。
  ただし 1-8 のオフセット配管が前提で、工数が大きい。**Phase 2 で
  十分な数字が出たらやらない。**

---

## 5. 順序と中止条件

```
Phase 0 (計装 + トレース)
   └─ ヒット率カーブが出ない / 期待値が低い → Phase 2 中止、Phase 1 と 5 だけやる
Phase 1 (起動時間)        ← 独立。いつでも入れられる
Phase 2 (スロット 96)     ← 本命
Phase 3 (先読み + rdadvise + カウンタ汚染)
Phase 4 (チャンク上限 + .q 投影)
Phase 5 (文脈長)          ← 独立。いつでも入れられる
Phase 6 (投機的)          ← Phase 2 の数字を見てから
```

### 実施後の残り

```
Phase 5 (文脈長)                    ← 独立・低コスト。次はここ
decode の未計装 26 ms/token の分解  ← PLAN 外。I/O 律速を脱したので唯一の残り
Phase 4 (チャンク上限 + .q 投影)     ← TTFT はほぼ全部 prefill になった
Phase 3 (TTFT 目的の先読み)         ← 目的が変わった。Phase 4 の後で再評価
Phase 6                             ← やらない
```

---

## 6. 測定プロトコル (status 9 節を踏襲)

- Tsugumi のプロセスは同時に 1 つだけ
- サーマルドリフト (連続実行で約 4% 低下) があるので条件はインターリーブ、
  実行間に 20 s クールダウン
- `swift run` ではなく `.build/release/` を直叩き
- **サンプリングはモデル推奨値 = CLI の既定値に固定する:
  `--temperature 1.0 --top-k 64 --top-p 0.95`。** 種は run 番号 (`--seed $n`)
  にして、run ごとの変動は許容する (同じ run 番号なら再現する)。
  greedy (`--temperature 0`) は決定的で都合はよかったが、実際の生成条件では
  なく、このモデルはループに落ちる。**temperature 0.2 は廃止した**
  (RESULTS §3-6)
- 3 回の中央値。1 回の値は載せない。**tok/s の run 間の振れは ±1-4%** (**実測**)
- 既存の `bench.sh` / `bench/results.tsv` を使う。Phase 0 で footer が変わったら
  パーサを追従させる (現在は `/usr/bin/time -p` の `real` からの引き算に依存
  しているが、footer に prefill 秒が出れば引き算自体が不要になる)

各 Phase の合格条件は「12 GB を超えないこと」と「decode tok/s が
インターリーブ 3 回中央値で改善すること」の 2 つ。

---

## 7. リスク

実施後の状態を各行の末尾に追記した。

| リスク | 内容 | 対策 | 実施後 |
| --- | --- | --- | --- |
| 匿名メモリの圧縮/スワップ | (下記) | (下記) | **顕在化せず。** 実測 peak 7.1 GB / 18 GB で圧がかからない |
| Metal 作業セット超過 | (下記) | (下記) | **ガードが機能。** 112 slots = 12.96 GB を起動時に拒否 (**実測**) |
| 先読みで起動が遅くなる | (下記) | (下記) | **先読みは実装せず。** 代わりに充填コスト +2.4 s が判明 (Phase 3 参照) |
| LFU カウンタの飽和 | (下記) | (下記) | **リスクなし。** `expertUseCount` は `[Int]` = 64bit で現実的に飽和しない |
| 上流との乖離 | (下記) | (下記) | 既定値変更 + ホットパス書き換えで乖離した。想定どおり |

以下は当初の記述。

| リスク | 内容 | 対策 |
| --- | --- | --- |
| 匿名メモリの圧縮/スワップ | 9.7 GB の expert キャッシュは `posix_memalign` の匿名メモリ。メモリ圧がかかると OS が圧縮を始め、SSD 読みより遅くなる | 予算 12 GB を守る。実行前に `memory_pressure -Q` を確認。長期的には Phase 6 の mmap 化 |
| Metal 作業セット超過 | `recommendedMaxWorkingSetSize` = 12.88 GB を超えるとドライバ側の挙動が不利になる (**未確認**、超えたことがない) | Phase 2 のメモリガードで起動時に弾く |
| 先読みで起動が遅くなる | 9.7 GB の先読みに 5-10 s | バックグラウンドで、最初のトークン生成と並行させる。サーバ常駐前提なら問題にならない |
| LFU カウンタの飽和 | `expertUseCount[expert] &+= 1` にラップ対策なし。長時間セッションで飽和すると追い出しが壊れる (**未確認**) | Phase 2 で読む。周期的な半減 (aging) を入れる |
| 上流との乖離 | 許可リストを広げるだけなら上流互換。既定値の変更とホットパス書き換えは乖離する | `macos15-support` ブランチで進める。上流に戻す気がないことは前提 |

---

## 8. 却下した案

- **macOS 26 へのアップグレード**: 0-1 で決定済み。MPP で prefill 約 12% が
  戻るが、この機体では未測定の他機体の値。決めた以上は蒸し返さない。
- **全 128 エキスパート常駐**: 14.5 GB。Metal 推奨作業セットを超え、
  llama.cpp と同じ「動くが常用できない」状態に戻るだけ。目的に反する。
  → **却下の根拠が増えた。** 仮に載ったとしても decode ヒット率は
  99.3% で 64 スロットの 99.2% と変わらず、tok/s も変わらない (**実測/導出**)。
- **エキスパートの追加量子化 (4bit → 3bit など)**: モデル品質を落とす改造は
  今回の目的 (同じモデルを軽く動かす) の外。

## 付録　計測用プロンプト
以下の複数のプロンプトで日本語を生成する際のpp,tg性能を測定すること。gemma-4-26b-a4b は日本語性能が突出して高く、常駐の目的はそこにある。
逆にコーディングタスクは別のLLMサーバーを呼ぶので、あまり必要ない。

> **測定済み (64 スロット固定、384 tok 生成、インターリーブ 3 回中央値)。**
> 数字と考察は `RESULTS.md` §3-4、再実行は `./bench.sh japrompts && ./bench.sh ja`
> (プロンプトは `bench/haiku.json` / `bench/math.json` / `bench/story.json`)。
>
> | prompt | ptok | tg tok/s | TTFT | decode hit |
> | --- | ---: | ---: | ---: | ---: |
> | 寿司俳句 | 510 | 30.84 | 9.91 s | 99.2% |
> | デカ妹創作 | 96 | 31.31 | 2.00 s | 98.5% |
> | 三次方程式 | 43 | 28.42 | 1.54 s | 97.4% |
>
> **短いプロンプトほど tg が落ちる** (prefill がキャッシュを温めないため、
> −8%)。§0 の 31.02 tok/s は長めのプロンプト固有の値で、常用レンジは
> **28-31 tok/s / TTFT 1.5-10 s**。
>
> **温度は性能に効かない** (0.2 と 1.0 の差は run 間の振れ ±1-4% の中、
> `head` も一定なので top-k/top-p のコストは出ない)。**品質だけで決めてよく、
> 品質では推奨の 1.0 が明確に良い**: 俳句プロンプトを 1024 tok で回すと
> temp 0 は「あわび (3)」を 25 回繰り返してループ、0.2 も答えに到達せず、
> **1.0 だけが 828 tok で自分で止まって 5・7・5 を完答した** (RESULTS §3-5)。
>
> → **既定値を CLI / サーバ / アプリ / ベンチのすべてで 1.0 に変更し、
> 0.2 は廃止した** (RESULTS §3-6)。`--temperature 0` はフラグとしては残す。

# 寿司俳句テスト（ヒント付き）



## 指示

これから、日本語の能力と思考力を測るテストを行います。

以下の厳密なルールに従って、俳句（5・7・5）を一つ作成してください。



## ルール

1. **使用できる言葉:** 寿司ネタ（例：まぐろ、いか、たまご、えび、うに、いくら、あなご等）の名前**のみ**を使用してください。

2. **禁止事項:** 寿司ネタ以外の言葉（助詞「てにをは」、動詞、形容詞、接続詞など）は**一切使用しないでください**。

3. **形式:** 句読点は不要です。上の句（5モーラ）、中の句（7モーラ）、下の句（5モーラ）の形式を守ってください。文字数ではなく**モーラ（拍）**で数えてください。

* **モーラ数の例:**

* いか (i-ka): 2モーラ

* えび (e-bi): 2モーラ

* うに (u-ni): 2モーラ

* まぐろ (ma-gu-ro): 3モーラ

* たまご (ta-ma-go): 3モーラ

* あなご (a-na-go): 3モーラ

* いくら (i-ku-ra): 3モーラ

* ほたて (ho-ta-te): 3モーラ

* サーモン (sa-a-mo-n): 4モーラ

* かんぱち (ka-n-pa-chi): 4モーラ

* うなぎ (u-na-gi): 3モーラ

* (長音「ー」、促音「っ」、撥音「ん」もそれぞれ1モーラとして数えます)

4. **季語:** 季語を入れる必要はありません。ルール1と3を最優先してください。



## 思考プロセスについて

このテストでは、寿司ネタの名前をリストアップし、そのモーラ数を正確に数え、それらを指定されたモーラ数（5・7・5）に合うように組み合わせる論理的な思考プロセスが重要になります。上記のモーラ数の例を参考にしてください。



## 作成指示

上記のルールとモーラ数の例を参考に、寿司俳句を一つ作成してください。


# 理不尽大学入試問題

x^3+2x^2-3x-1=0を解け
（2126年大学入試問題）

# デカ妹創作

以下の文章の続きを書きショートストーリーにして

ある日突然あなたに12人もの妹ができたらどうしますか？
それも…
とびっきり可愛くて
とびっきり素直で
とびっきり愛らしくて
とびっきりの淋しがりや
しかもそのうえ…
彼女たちはみんなみんなとびっきり！
タッパがデカいんです…
