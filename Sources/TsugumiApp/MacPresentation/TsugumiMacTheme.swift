import AppKit
import SwiftUI

public enum TsugumiMacTheme {
    public static let accentNSColor = NSColor(
        srgbRed: 106.0 / 255.0,
        green: 186.0 / 255.0,
        blue: 113.0 / 255.0,
        alpha: 1)

    public static let accentColor = Color(nsColor: accentNSColor)
}
