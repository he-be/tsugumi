# RESULTS_QAT — lattice-aligned QAT チェックポイントの受入結果

実施: 2026-08-16 / M3 Pro 18GB / macOS 15.7.5 / `macos15-support` ブランチ
commit `7a6b5ff` (Phase A+B+C 適用後)
Apple Swift 6.2.4 / target arm64-apple-macosx15.0
プロトコル: PLAN §6 (`.build/release` 直叩き、インターリーブ + 20s クールダウン、
3 回の中央値)。PLAN_QAT §5 の受入チェック。

表記は PLAN と同じ: **実測** / **導出** / **未確認**

---

## 0. 結論 — **見送りを推奨** (**実測**)

PLAN_QAT §5-2 の 5 つのゲートのうち、**性能と品質の 2 つが不合格**。
PLAN_QAT §7 の中止条件「受入で tg が −10% 超、かつ品質ゲートもベースライン
並みに見えない → 導入見送りを報告」にそのまま該当する。

| # | ゲート | 判定 | 実測 |
| --- | --- | --- | --- |
| 1 | 生成の目視 (破綻・ループなし) | **合格** | 日本語は自然、ループなし |
| 2 | 品質 (寿司俳句 / 三次方程式) | **不合格** | 俳句のモーラ数を 3/3 で誤る。ベースラインは 3/3 正答 |
| 3 | 性能 (tg ≥ −10%、TTFT +1s 以内) | **不合格** | tg −10.9〜−16.5%、TTFT +3.43s |
| 4 | メモリ (peak < 12GB、64 スロット) | **合格** | peak 7.84GB、64 スロット通過 |
| 5 | `trusted-install` / `full-sha256` 両起動 | **合格** | 両方 exit 0 |

**ゲート 2 が決定的。**PLAN_QAT §5-2-3 は「−10% ちょうど〜わずかに超過なら、
品質がベースラインより**明確に良ければ**採用可」と事前に取り決めていた。
実測は逆で、俳句課題では**ベースラインのほうが明確に良い**。
「速度が落ちたので品質ゲートを甘く見る」順序は取らない、という取り決めどおり
見送りとする。

**ただし Phase A/B/C の実装はすべて有効で、残す。**group 32/64 両対応、
BF16 ルーター、ローカルスナップショット repack はいずれも正しく動いており、
この受入判定が成立したのはそれらが動いたからである (§1)。

---

## 1. インストール (PLAN_QAT §5-1) — 合格 (**実測**)

```bash
./.build/release/TurboFieldfareRepack --output scratch/gemma4-qat.gturbo \
  --source-snapshot scratch/qat-aligned-snapshot
# exit 0 / 40.6s (user 13.48s, sys 6.02s)
# Installed Gemma 4 26B-A4B IT QAT q4_0 (lattice-aligned)
# Source revision: 745a97a754ed4b7713163c7d0e9c11da41809e0c
# Model: /Users/mh/LLM/turbo-fieldfare/scratch/gemma4-qat.gturbo
```

**PLAN_QAT §2 の試算が実物と厳密に一致した** (**実測**):

| 項目 | §2 の予想 | 実測 | |
| --- | ---: | ---: | --- |
| expertStride | 3,719,168 B | **3,719,168 B** | 一致 |
| layer file | 476,053,504 B | **476,053,504 B** | 一致 |
| resident | 1,512,804,412 B | 1,512,886,332 B | +81,920 B (index のページ埋め) |
| 合計 | — | 15,835,165,158 B (37 files) | |

manifest の検証 (**実測**):

```json
"router":       {"weightBits":16,"scheme":"bf16","scaleType":"none","biasType":"none","groupSize":32}
"embedding":    {"weightBits": 4,"scheme":"affine","scaleType":"BF16","biasType":"BF16","groupSize":32}
"sharedExpert": {"weightBits": 4, ...}   ← 現行ピンは 8
"modelID": "mlx-community/gemma-4-26B-A4B-it-qat-q4_0-mlx-aligned"
"sourceSnapshotHash": "sha256:7dbbeef0…"
```

`--verify-install` は 37 ファイル 15,835,165,158 バイトを検証して exit 0。

### 1-1. トークナイザは同一だった (**実測**、交絡がないことの確認)

`tokenizer.json` の SHA-256 が現行ピンと**バイト単位で同一**。
差があるのは `tokenizer_config.json` (`local_files_only`、`tool_parser_type`) と
`chat_template.jinja` (ツール呼び出し内のマルチモーダル分岐 9 行) のみで、
**テキスト専用のプロンプト整形経路には差がない**。
→ 以下の品質比較はプロンプト整形の違いで交絡していない。

---

## 2. 性能 (PLAN_QAT §5-2-3) — 不合格 (**実測**)

`--temperature 1.0 --top-k 64 --top-p 0.95 --seed <run>`、`--max-new 384`、
`--max-context 4096`、64 スロット、prefill on / chunk 128、rdadvise off、
`trusted-install`。base と qat を**プロンプトごとに交互に**回し、
各実行後 20s クールダウン。3 回の中央値。

| prompt | base | qat | Δ | 判定 |
| --- | ---: | ---: | ---: | --- |
| haiku | 30.215 | *20.787* | *測定不能* | §2-1 |
| math | 29.430 | **24.587** | **−16.5%** | 不合格 |
| story | 31.421 | **27.992** | **−10.9%** | 不合格 |

TTFT (haiku、510 tok プロンプト): **9.649s → 13.082s = +3.43s**。
ゲートは「+1 s 以内」なので不合格。prefill の expert リクエストが
7,854 → 9,545 件 (+21.5%) に増え、prefill ヒット率が 42.5% → 17.9% に落ちている。

peak は 7.15 → 7.84 GB。12 GB 予算内 (§4)。

### 2-1. haiku 行が測定不能な理由 (**実測**)

**QAT モデルは haiku プロンプトに 20-23 トークンで答えて `endOfTurn` で停止する**
(temp 0 / temp 1.0、6 回すべて)。ベースラインは 384 tok を使い切る (`maxTokens`)。
20 トークンの tok/s は初動 (コールドな expert キャッシュ、io=9.0ms/tok) に
支配されるので、**384 tok の値と比較してはいけない**。
これは速度差ではなく生成長の差であり、§3 の品質評価の材料である。

PLAN_QAT §5-2-3 のゲート値「haiku tg ≥ 27.8 tok/s」は、この理由で
**このプロンプトでは評価できない**。384 tok を完走する math / story で判定した。

### 2-2. 内訳 — 予想どおりの部分と、予想外の部分 (**実測** + **導出**)

footer の `decode/tok` 内訳 (math、run 1):

| | base | qat | Δ |
| --- | ---: | ---: | ---: |
| decode 合計 | 34.0 ms/tok | 41.2 ms/tok | +7.2 |
| io (expert 読み出し) | 5.51 | 9.34 | **+3.83** |
| cb1 / cb2 | 0.66 / 0.23 | 0.67 / 0.24 | ±0 |
| head | 3.18 | 3.52 | +0.34 |
| 未計装 (routed expert GEMV 他) | 24.4 | 27.4 | +3.0 |

- **未計装ぶん +3.0 ms (+12%) は PLAN §2 の予想どおり。**expert バイト +10.7%、
  スケール帯域 2 倍という試算と整合する。ここだけなら −6〜−7% に収まっていた。
- **予想外は io。**decode ヒット率が math で 97.7% → 96.2% に落ちた
  (ミス 2,125 → 3,527 件、+66%)。1 ミスあたりのバイトが +10.7% 増えた上に
  ミス回数そのものが増えたので、io が +69% になった。
  ヒット率が保たれた story (98.5% → 98.3%) は −10.9% で収まっている。
- ヒット率の低下は**ルーティングがモデルごとに違うから**であり、
  カーネルやキャッシュの不具合ではない (§5-A は 20/20 PASS のまま)。

**含意 (**導出**):** 性能差の半分は「group 32 で読むバイトが増えた」ぶんで
予想どおり、もう半分は「このチェックポイントのルーティングが 64 スロットに
乗りにくい」ぶん。後者はスロットを増やせば縮むが、§2 の予算では 80 スロットで
余白が 2 GB を切る。

---

## 3. 品質 (PLAN_QAT §5-2-2) — 不合格 (**実測**)

`--temperature 1.0 --top-k 64 --top-p 0.95`、`--max-new 1024`、seed 1/2/3。
base と qat を交互に、20s クールダウン。

### 3-1. 寿司俳句 — **ベースラインが明確に良い**

課題は「寿司ネタの名前のみで 5・7・5 の俳句を作る。文字数ではなくモーラで数える」。

| seed | ベースライン | モーラ | QAT | モーラ |
| ---: | --- | --- | --- | --- |
| 1 | まぐろえび / サーモンほたて / うにあなご | **5-7-5** ✓ | まぐろいくら / うにたまごあなご / えびほたてうに | 6-8-7 ✗ |
| 2 | まぐろえび / サーモンいくら / うにたまご | **5-7-5** ✓ | まぐろいくら / うにたまごえび / あなごほたて | 6-7-6 ✗ |
| 3 | まぐろうに / かんぱちいくら / あなごえび | **5-7-5** ✓ | まぐろうにいくら / サーモンたまごえび / あなごうにいくら | 8-9-8 ✗ |

**ベースライン 3/3 正答、QAT 0/3。**

原因は明確で、**QAT は思考過程を書かずに即答する**。ベースラインは
ネタごとのモーラ数を列挙し、和を計算し、最後に検算してから答える
(589-828 tok)。QAT は 20-23 tok で答えだけを出し、モーラを数えていない。
語彙・語形は正しい (寿司ネタのみ、助詞なし) ので、**日本語の生成品質ではなく
「数えるべき課題で数えない」という振る舞いの差**である。

> temp 0 では逆にベースラインが「あわび」ループに落ちて 384 tok で答えに
> 到達しなかった (QAT は同じ 6-7-6 を即答)。これは `bench.sh` のコメントが
> 記録している既知の temp 0 アーティファクトで、ゲートの温度 (1.0) では
> 起きない。**ゲートは temp 1.0 で判定した。**

### 3-2. 三次方程式 — 引き分け

`x^3+2x^2-3x-1=0`。両者とも有理根がないことを確認し、`x = y - 2/3` で
2 次項を消し、三角関数表示 (カルダノの三角関数形) に進む。**手法は両者妥当。**
1024 tok では**両者とも最終解に到達しない** (計算途中で打ち切り)。

**観察項目だったアラビア語トークン混入は、temp 1.0 では両モデルとも 0 件**
(3 seed × 2 モデルを Arabic / Cyrillic / Hangul / Devanagari で走査、**実測**)。
temp 0 のベースラインでのみ `$x = مقدار$` を観測した。
→ **「QAT で混入が消える」という期待は、そもそも temp 1.0 で混入が
起きないため検証できなかった。**

---

## 4. メモリ (PLAN_QAT §5-2-4) — 合格 (**実測**)

| | base | qat |
| --- | ---: | ---: |
| peak (haiku 510tok) | 7.15 GB | **7.84 GB** |
| peak (math / story) | 7.04 / 6.99 GB | 7.76 / 7.75 GB |

**64 スロットは `ExpertCacheBudget` を通った** (48 への降格は不要)。
12 GB 予算に対し余白 4.2 GB。

PLAN §2 は peak 約 9.3 GB と**導出**していたが、**実測は 7.84 GB で 1.5 GB 軽い**。
§2 は resident 増 + expert stride 増を単純加算していたが、peak は
キャッシュ常駐ぶんの実効値で決まるため。**予想が甘い側ではなく安全側に外れた。**

---

## 5. インストール検証 (PLAN_QAT §5-2-5) — 合格 (**実測**)

```
--verification trusted-install : exit 0, load=0.770s layerVerify=0.000s/30layers
--verification full-sha256     : exit 0, load=0.731s layerVerify=5.881s/30layers
```

---

## 6. プロトコルからの逸脱 (**実測**、すべて記録)

1. **§5-2-3 のゲート値 (haiku tg ≥ 27.8 tok/s) は評価しなかった。**
   QAT が haiku プロンプトで 20 tok 停止するため測定不能 (§2-1)。
   384 tok を完走する math / story で判定した。
2. **性能測定は temp 1.0 で行った。**§5-0 の回帰は RESULTS §3-4 と揃えるため
   temp 0 だったが、今回は base/qat を同一条件で交互に測る比較なので、
   §5-2-3 が指す `./bench.sh ja` の既定 (temp 1.0) に合わせた。
   temp 0 でも同じ 3 プロンプトを 3 回インターリーブしており、
   math −16.3% / story −10.2% で温度による結論の差はない (**実測**)。
3. **`docs/RUNTIME_CONTROLS.md` は更新していない。**PLAN_QAT §4 Phase C の
   ファイル一覧に挙げていたが、同ファイルは生成・ランタイムのフラグのみを
   扱っており、インストーラの記述場所がない。README に書いた。
4. 品質ゲートは haiku / math の 2 プロンプト × 3 seed。story は性能のみ。

---

## 7. 残るもの / 残さないもの

**残す (すべて有効、PLAN_QAT §0-1 の判断どおり):**

- group 32/64 両対応 (Phase A/B)。§5-A は 20/20 PASS のまま。
- BF16 ルーター経路 (Phase B)。このチェックポイントを載せられた理由そのもの。
- `--source-snapshot` によるローカル repack (Phase C)。
- 既定ピン (`gemma-4-26b-a4b-it-4bit`) は**変更しない**。

**判断を仰ぐもの:**

- `scratch/gemma4-qat.gturbo` (15 GB) と `scratch/qat-aligned-snapshot` (15 GB) の
  保持。見送りが確定するなら 30 GB を解放できる。
