# RESULTS_QAT — lattice-aligned QAT チェックポイントの受入結果

実施: 2026-08-16 / M3 Pro 18GB / macOS 15.7.5 / `macos15-support` ブランチ
commit `7a6b5ff` 以降 (Phase A+B+C 適用後、`--thinking` 追加後)
Apple Swift 6.2.4 / target arm64-apple-macosx15.0
プロトコル: PLAN §6 (`.build/release` 直叩き、インターリーブ + 20s クールダウン、
3 回の中央値)。PLAN_QAT §5 の受入チェック。

表記は PLAN と同じ: **実測** / **導出** / **未確認**

---

## 0. 結論 — **採用。48 スロットを既定にする** (**実測**)

| # | ゲート | 判定 | 実測 |
| --- | --- | --- | --- |
| 1 | 生成の目視 | 合格 | 日本語は自然、ループなし |
| 2 | 品質 | **合格** | 三次方程式は QAT が明確に上、俳句は引き分け (§3) |
| 3 | 性能 (tg ≥ −10%) | **不合格** | −17〜−25%。ただしゲート指標そのものが不適切 (§2-3) |
| 4 | メモリ | 合格 | 48 スロットで peak **6.0 GB** (12 GB 予算に対し余白 6 GB) |
| 5 | `trusted-install` / `full-sha256` 両起動 | 合格 | 両方 exit 0 |

採用の理由は、**tok/s が受入指標として誤っていた**ことがユーザー指摘で判明した点にある
(§2-3)。答えに到達しなければ再生成であり、それは −20% ではなく −100% である。
同一トークン予算で閉形式に到達したのは QAT 2/3 に対しベースライン 0/3 (§3-2)。

既定スロットは **64 → 48** に変更した (§4)。QAT は 48 で 64 と同速、peak は 1.8 GB
軽い。**この変更は group-64 のベースラインには約 4.5% のコスト**になる
(§4-2、既存の実測値であり本セッションでは再測していない)。

---

## 1. この受入は 2 回やり直している。1 回目は測定が誤っていた (**実測**)

**最初の判定は「品質不合格・見送り推奨」だった。それは誤りで、原因は測定系にあった。**

`GFTokenizer.applyChatTemplate` がチャットテンプレートの
`enable_thinking = false` レンダリングを**ハードコード**していた
(`Tokenizer.swift:325`、生成プロンプト末尾の `<|channel>thought\n<channel|>` =
thought チャネルを開いて即閉じる = 「考えずに答えろ」)。呼び出し側に選択肢はなかった。

その条件下で寿司俳句 (モーラ数を数える課題) を測ると、比べていたのは:

| | 挙動 | 結果 |
| --- | --- | --- |
| ベースライン | no-think 指示を**無視して**答案欄に「思考プロセス：」を書く | 数えるので 5-7-5 正解 3/3 |
| QAT | 指示に**従って** 20 トークンで即答 | 数えないので 3/3 不正解 |

**推論が必須の課題で、品質ではなくコンプライアンスの差を測っていた。**

修正として `applyChatTemplate(_:enableThinking:)` に `enable_thinking = true` の
レンダリング (システムターン先頭に `<|think|>`、生成プロンプトは thought チャネルを
閉じない) を追加し、`--thinking on|off` を CLI に出した。既定は従来どおり off で、
サーバと Mac アプリのレンダリングは不変。

> **教訓 (本 PLAN で最も高くついた発見):** 評価ハーネスが暗黙に固定していた設定が、
> 評価結果そのものを決めていた。§5-A のカーネル数値検証には同型の穴を 1 つ潰した
> 前例がある (PLAN_QAT §4 Phase A0 の `RelError.compute` が NaN を PASS にしていた件)。
> **モデルを比較する前に、ハーネスが何を固定しているかを列挙すべきだった。**

以下の §2 / §3 はすべて `--thinking on` で測り直したものである。

---

## 2. 性能 (**実測**)

### 2-1. 同条件インターリーブ (thinking on、384 tok、3 回中央値)

`--temperature 1.0 --top-k 64 --top-p 0.95`、64 スロット、prefill on / chunk 128、
rdadvise off、`trusted-install`。プロンプトごとに base / qat を交互。

| prompt | base | qat | Δ |
| --- | ---: | ---: | ---: |
| haiku | 26.553 | 21.286 | −19.8% |
| math | 26.792 | 19.602 | −26.8% |
| story | 27.926 | 23.447 | −16.0% |

thinking off でも測っており、math −16.5% / story −10.9%
(haiku は QAT が 20 tok で `endOfTurn` 停止するため測定不能)。
バッチ間には熱ドリフトによる散らばりがあり、**堅い言い方は「QAT の decode は
おおむね 20% 遅い」**である。

### 2-2. 原因は GPU 律速であり、スロットでは買い戻せない (**実測**、決定的)

QAT を 96 スロットまで積んだ:

| slots | prompt | tok/s | io ms | decode hit | peak |
| ---: | --- | ---: | ---: | ---: | ---: |
| base 64 | math | 30.227 | 6.48 | 95.1% | 7.09 |
| qat 64 | math | 23.928 | 12.37 | 90.6% | 7.77 |
| qat 80 | math | 22.895 | 10.31 | 95.3% | 9.58 |
| qat 96 | math | 23.517 | 6.66 | **97.7%** | 11.33 |

**96 スロットで QAT のヒット率はベースラインを上回り (97.7% 対 95.1%)、io も
同等以下 (6.66 対 6.48 ms) になるのに、tok/s は動かない。**
したがって損失は I/O ではなく、group 32 の expert GEMV そのもの
(expert バイト +10.7%、スケール帯域 2 倍 = PLAN §2 の**導出**どおり)。
RESULTS §4 の「expert I/O は shared-MLP の GPU work と重なっているので
I/O をゼロにしてもその分速くならない」がそのまま効いている。

**メモリを積んでも買い戻せない、恒久的な演算コストである。**

### 2-3. tok/s は受入指標として誤っていた (ユーザー指摘、2026-08-16)

PLAN_QAT §5-2-3 のゲートは `haiku tg ≥ 27.8 tok/s` という**スループット単独の
指標**だった。これは次の 2 点で機能しなかった。

1. **生成長がモデルで変わる。**QAT は haiku プロンプトに 20 tok で即答して停止する
   ため、384 tok 前提の tok/s が測れない。ゲート値そのものが評価不能だった。
2. **より本質的に、測るべきは正解到達までの時間である。**答えに到達しなければ
   再生成であり、コストは 2 倍 = −100%。トークン単価の −20% はその前に霞む。
   §3-2 のとおり、同一予算で閉形式に到達したのは QAT 2/3、ベースライン 0/3 で、
   **tok/s が速いほうが答えに遅い**という逆転が起きている。

**ゲートは「tok/s ≥ −10%」ではなく「正解到達時間」で定義すべきだった。**
この欠陥は PLAN_QAT §5-2-3 の設計に属する。

---

## 3. 品質 (**実測**、`--thinking on`、temp 1.0 / top-k 64 / top-p 0.95、seed 1-3)

### 3-1. 寿司俳句 — 引き分け

「寿司ネタの名前のみで 5・7・5。文字数ではなくモーラで数える」。

`--max-new 6000 --max-context 8192` で両モデルを回したが、**どちらも自然停止しない**。
ループではなく、正しい推論を延々と再検算し続ける (最頻 40 文字窓が 2 回、
= 反復なし)。生成中に書かれた三行候補をモーラ辞書で機械採点した結果:

| | 三行候補 | うち 5-7-5 |
| --- | ---: | ---: |
| ベースライン (seed 1-3) | 12 | **12** |
| QAT (seed 1-3) | 5 | **5** |

(QAT seed 2 は候補をインライン記法で書いたため機械抽出に載らないが、
本文中で `うにいくら(5) / サーモンたまご(7) / えびまぐろ(5)` を検算済み。)

**両モデルとも正解を出せる。この課題はモデル間を分離しない。**
むしろ候補を多く書き出したのはベースラインである。

### 3-2. 三次方程式 — **QAT が明確に上**

`x^3+2x^2-3x-1=0`。2048 tok / thinking on で、解の各構成要素が出現した位置:

| | p=−13/3 | q=43/27 | k=2√13/3 | cos3θ | arccos |
| --- | --- | --- | --- | --- | --- |
| base s1 | 41% | 42% | — | — | — |
| base s2 | 26% | 26% | 66% | 81% | — |
| base s3 | 34% | 34% | — | — | — |
| **qat s1** | 33% | 33% | 56% | 62% | **68%** |
| **qat s2** | 28% | 27% | 57% | 62% | **63%** |
| qat s3 | 34% | 35% | 78% | 86% | — |

**同一トークン予算で閉形式に到達したのは QAT 2/3、ベースライン 0/3。**
QAT s1 の到達形は
`x = (2√13/3)·cos((1/3)·arccos(−43/(26√13)) + 2nπ/3)` で、
`p = b − a²/3 = −13/3`、`q = 2a³/27 − ab/3 + c = 43/27`、
`cos3θ = −4q/k³ = −43/(26√13)` を**私の側でも検算し一致**した。
同じ予算でベースラインは s1/s3 で `k` にすら届かず、
「too messy」と述べて数値的な根の範囲探索に戻っている。

### 3-3. 観察項目

- **台本混入 (アラビア語トークン) は thinking on では両モデルとも 0 件**
  (3 seed × 2 モデル × 2 課題を Arabic / Cyrillic / Hangul / Devanagari で走査)。
  temp 0 のベースラインでのみ `$x = مقدار$` を観測した。
  PLAN_QAT §5-2-2 が期待した「QAT で混入が消える」は、**ゲートの設定では
  そもそも混入が起きないため検証できなかった。**
- temp 0 ではベースラインが俳句で「あわび」ループに落ちる。`bench.sh` が既知として
  記録している temp 0 アーティファクトで、ゲートの温度では起きない。
  **ベースラインの弱点の根拠には使わない。**

### 3-4. 品質判定の射程 (**重要**)

言えるのは**「今回測った範囲では、品質面で非 QAT を積極的に選ぶ理由は
見つからなかった」**までである。「品質的に採用理由なし」とは書けない。

- 標本は **2 課題 × 3 seed** のみ。優位が出たのは三次方程式 1 問、
  俳句は引き分け。
- **QAT は shared expert が 4bit、現行ピンは 8bit。**PLAN_QAT §6 がリスクとして
  挙げた項目だが、**これを狙った検証はしていない** (**未確認**)。
- 日本語・推論偏重。長文脈、コード、翻訳、事実想起、指示追従の細部は未測定。

---

## 4. スロット数 — 既定を 64 から 48 へ (**実測**)

### 4-1. QAT のスロット感度

thinking on、384 tok、3 回中央値、qat 32/48/64 をインターリーブ。

| slots | prompt | tok/s | io ms | decode hit | peak | vs 64 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: |
| 32 | haiku | 19.996 | 19.53 | 77.5% | 4.22 | −6.2% |
| **48** | haiku | 21.689 | 14.89 | 87.4% | **6.03** | **+1.8%** |
| 64 | haiku | 21.315 | 11.62 | 93.5% | 7.82 | — |
| 32 | math | 19.270 | 23.71 | 72.1% | 4.22 | −7.4% |
| **48** | math | 21.057 | 17.68 | 83.2% | **5.98** | **+1.2%** |
| 64 | math | 20.803 | 15.62 | 90.6% | 7.80 | — |

48 は 64 と同速 (±2% は run 間の振れの中) で **peak が 1.8 GB 軽い**。
ヒット率が 93.5% → 87.4% に落ちて io が増えても速度が動かないのは §2-2 と同じ理由。
崖は 32 にあり、そこで初めて I/O が GPU work の裏に隠れきらなくなる。

**含意 (**導出**):** QAT から約 20% を奪っているのと同じ性質 —
group 32 で expert GEMV が重い — が、**より小さいキャッシュへの耐性を買っている**。
ベースラインの knee が 64 なのに QAT が 48 で足りるのはそのため。

### 4-2. 既定変更のコストと影響範囲

`RuntimeConfiguration` の既定を **64 → 48** に変更した。CLI / サーバ /
decode service が共有する production 既定である。

- **QAT**: 速度不変、peak 7.8 → 6.0 GB。
- **group-64 ベースライン**: **約 4.5% 遅くなる**。64 はベースラインの knee
  (decode hit 99.2%) で、これは既定が 64 だったときの実測値であり
  **本セッションでは再測していない** (**未確認**)。`--expert-cache-slots 64` で戻せる。
- Mac アプリの `AppRuntimeOptions` 既定 (16) は別の設定で、**変更していない**。

---

## 5. インストール (PLAN_QAT §5-1) — 合格 (**実測**)

```bash
./.build/release/TsugumiRepack --output scratch/gemma4-qat.moepack \
  --source-snapshot scratch/qat-aligned-snapshot
# exit 0 / 40.6s
# Source revision: 745a97a754ed4b7713163c7d0e9c11da41809e0c
```

**PLAN_QAT §2 の試算が実物と厳密に一致した:**

| 項目 | §2 の予想 | 実測 | |
| --- | ---: | ---: | --- |
| expertStride | 3,719,168 B | **3,719,168 B** | 一致 |
| layer file | 476,053,504 B | **476,053,504 B** | 一致 |
| resident | 1,512,804,412 B | 1,512,886,332 B | +81,920 B (index のページ埋め) |

manifest: router = `{16, "bf16", "none", "none", groupSize 32}`、
embedding / attention / sharedExpert / routedExpert = 4bit affine group 32。
sharedExpert が 4bit (現行ピンは 8bit) であることを確認。
`--verify-install` は 37 ファイル 15,835,165,158 B を検証して exit 0。

**トークナイザは現行ピンとバイト単位で同一。**差は `tokenizer_config.json` の
2 キーと `chat_template.jinja` のツール呼び出し内マルチモーダル分岐 9 行のみで、
テキスト専用のプロンプト整形経路に差はない。
→ §3 の品質比較はプロンプト整形で交絡していない。

peak は §2 の**導出** 9.3 GB に対し 64 スロットで 7.8 GB、48 スロットで 6.0 GB。
**試算は安全側に外れた** (キャッシュ常駐の実効値で決まるため)。

---

## 6. インストール検証 (PLAN_QAT §5-2-5) — 合格 (**実測**)

```
--verification trusted-install : exit 0, layerVerify=0.000s/30layers
--verification full-sha256     : exit 0, layerVerify=5.881s/30layers
```

---

## 7. プロトコルからの逸脱 (**実測**、すべて記録)

1. **§5-2-3 のゲート値 (haiku tg ≥ 27.8 tok/s) は満たしていない。**
   採用はゲートを満たしたからではなく、**ゲート指標が誤っていたと判断したから**
   である (§2-3)。この判断はユーザーの決定。
2. **1 回目の測定は無効。**`enable_thinking` がハードコードされており、
   品質ゲートの結論が反転した (§1)。
3. **品質ゲートの「完答すること」は両モデルとも満たさない。**俳句は 6000 tok でも
   自然停止しない。判定は「書かれた候補の正しさ」に読み替えた。
4. **性能測定は temp 1.0。**§5-0 の回帰は RESULTS §3-4 と揃えるため temp 0 だったが、
   本比較は base/qat 同一条件の交互測定なので `./bench.sh ja` の既定に合わせた。
5. **`docs/RUNTIME_CONTROLS.md`** は PLAN_QAT §4 Phase C の一覧に挙がっていたが、
   インストーラの記述場所がないため README に書いた。今回スロット既定の行のみ更新。
6. 品質は haiku / math の 2 課題。story は性能のみ (§3-4)。
7. ベースラインの 48 スロットでのコスト (約 4.5%) は既存記録の引用で、
   本セッションでは再測していない (§4-2)。

---

## 8. 残作業

- **既定ピンの切り替え** (`SupportedModelSource`) は未実施。ランタイムは両対応
  なので独立に判断できる (PLAN_QAT §0-1)。
- shared expert 4bit を狙った品質検証 (§3-4)。
- ベースライン 48 スロットの再測 (§4-2)。
- `scratch/qat-aligned-snapshot` (15 GB) は repack 済みなので削除可能。
